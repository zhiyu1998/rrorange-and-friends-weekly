---
title: 第039期 - 花间橘影
date: 2026-01-23
cover: https://i.imgur.com/7k5DGIK.jpeg
created: 2026-01-23 14:20:00
updated: 2026-06-19 00:00:00
---

# 第039期 - 花间橘影

![](https://i.imgur.com/7k5DGIK.jpeg)

## \>\> 🐈大橘的工具集

### [Biu - B站跨平台桌面音乐播放器](https://github.com/wood3n/biu)

一款基于哔哩哔哩公开接口开发的非官方跨平台桌面音乐播放器（Electron + React + TypeScript 构建），主打纯净无广告的听歌体验。登录后可同步 `收藏夹/稍后再看/历史记录/用户投稿`，并优先拉取更高码率音频流（含无损 `Flac`、`Hi-Res`，部分需大会员）。它提供 `mini 播放器` 模式和系统托盘控制，支持视频提取音频、单个或收藏夹批量下载、全屏歌词显示，还内置音乐推荐（新歌热榜、分区推荐）和本地音乐播放功能。界面支持浅色/深色主题切换、侧边栏收藏夹折叠与拖拽排序，覆盖 Windows/macOS/Linux 三平台，采用 PolyForm Noncommercial 许可证，适合把 B 站当作音乐库重度使用的用户。

![](https://i.imgur.com/d7c2uTi.jpeg)

### [Agent Arena - AI Agent 真实性能排行榜](https://arena.ai/leaderboard/agent)

一个基于「真实在野交互」的 AI Agent 性能排行榜，数据直接来自平台的 `Agent Mode`（截至 2026 年 6 月已积累超 75.4 万个会话、27 个模型、数百万次工具调用），而非静态基准或人工投票。它用 `Causal Tracing 因果追踪`方法把主模型、子代理、工具链的影响解耦，给出每个模型的 `Net Improvement 净提升百分比`（含 95% 置信区间），并可按 `任务成功`、`可操控性`、`Bash 恢复速度`、`工具幻觉率`等 5 个信号分别排序，是目前最贴近真实生产环境的 Agent 评估框架之一。

![](https://i.imgur.com/u3VeIlZ.png)

### [CursorBench - Cursor 官方 AI 编程评测基准](https://cursor.com/cn/cursorbench)

Cursor 官方自建的 AI 编程评测基准（当前版本 `CursorBench 3.1`），专门用来衡量 AI 编码代理在真实开发场景下的综合能力。它用自研的 `Cursor Blame`工具从真实使用会话中回溯任务（把工程师的原始 prompt 与最终 merge 的代码配对作为金标准），任务规模约为 `SWE-Bench Verified`的 2 倍，且刻意保留模糊、多文件、长周期的真实工程特征；评分也不仅看正确性，还综合 `代码质量`、`效率（token/成本/步数）`和 `交互行为`四个维度，用 AI grader 而非死板测试用例打分。官方主张公开基准已严重饱和且训练数据污染，CursorBench 在前沿模型间区分度更高、与线上真实表现高度一致，也常被开发者当作新模型发布的「验收标准」。

![](https://i.imgur.com/AXrVsdj.png)

### [Snippai - AI 智能截图识别工具](https://github.com/xyTom/snippai)

一款主打「Snip Anything Solve Everything」的开源截图工具，核心卖点是 `promptless`——截图后无需写任何提示词，内置的 OCR 与 Vision 大模型会自动判断内容类型并直接给出可用的结构化结果：数学公式转 `LaTeX`、表格转 `Markdown/Excel`、代码片段附带解释与重构建议、外语文本自动翻译，还能识别图片主色调生成配色方案。基于 Electron + React + TypeScript 构建，支持 Windows 与 macOS（含 ARM64），AGPL-3.0 协议开源，常被看作 Snipaste、Snagit、ShareX 这类传统截图工具的 AI 进化版，对敲公式、整理资料等高频重复劳动提效明显。

![](https://i.imgur.com/c8FBIZu.png)

### [Laragon - Windows 便携式本地开发环境](https://laragon.org/download)

一款专为 Windows 打造的便携式本地开发环境，被视作 `XAMPP/WAMP` 的现代化替代品，核心程序不到 6MB、运行时仅占 4-10MB 内存，不依赖 Windows 服务而是用自己的异步服务编排，启动极快、几乎不崩溃。它支持 `PHP`、`Node.js`、`Python`、`Go` 等多种技术栈，最大亮点是「自动虚拟主机」——在 `www` 目录建个项目文件夹，重启后就能直接用 `project.test` 这样的域名访问，自动配好 hosts 和 Apache/Nginx；还能一键切换多个 PHP/Node 版本、一键启用 SSL、通过 ngrok 把本地项目暴露到公网，整个环境塞进一个文件夹即可随 U 盘带走，不污染系统注册表。

![](https://i.imgur.com/irGalaa.png)

### [MicYou - 把 Android 手机变成 PC 麦克风](https://github.com/LanRhyme/MicYou/)

一款开源工具，能把闲置的 Android 手机秒变成电脑的高质量麦克风，省去单独购买麦克风硬件的开销。它用 Kotlin Multiplatform + Jetpack Compose 构建，支持 `Wi-Fi` 无线和 `USB`（ADB/AOA）两种连接方式，并内置 `噪声抑制`、`自动增益控制`、`去混响` 等专业音频处理链路，比直接传输更干净；桌面端配合 VB-Cable、BlackHole 等虚拟声卡，就能让 Zoom、OBS、Discord 等任意软件把手机识别为系统默认输入设备。全平台覆盖（Android + Windows/macOS/Linux），GPL-3.0 开源，特别适合笔记本自带麦效果差、临时开会开黑或直播录音的场景。

![](https://i.imgur.com/fjiKm7I.png)


### [Skills Hub - Hermes Agent 的自进化技能生态](https://hermes-agent.nousresearch.com/docs/skills)

Nous Research 出品的 Hermes Agent 配套技能中心，背后是一套会「越用越聪明」的 AI Agent 框架。它的核心机制是 `学习循环`：Agent 每完成一次复杂任务，就会自动把经验沉淀成一个可复用的 `SKILL.md` 技能（含使用时机、步骤、陷阱、模板），并在后续使用中不断迭代改进，相当于给 Agent 装上了「程序性记忆」。Skills Hub 遵循 `agentskills.io` 开放标准，支持浏览、搜索、一键安装官方与社区技能，还能用 `Skill Bundles` 把多个技能打包加载；安全层面有数据外泄/命令注入扫描和 builtin/official/community 分级信任。配合跨会话的 `持久记忆`、子代理并行、cron 定时任务等能力，是目前开源 Agent 里「自我进化」特征最鲜明的方案之一。

![](https://i.imgur.com/kd7kQ9h.jpeg)

### [Litestar - 高性能 ASGI Python Web 框架](https://github.com/litestar-org/litestar)

一款轻量但全功能的 ASGI Python 框架（前身是 Starlite），主打「异步优先但同步代码零惩罚」的设计，默认用 Rust 加速的 `msgspec` 做数据验证和序列化，在多数基准测试中吞吐量优于 FastAPI。它内置了 `依赖注入`、`OpenAPI 3.1 自动文档生成`、`SQLAlchemy 深度集成`（含 DTO 模式）、`WebSockets`、`CORS/CSRF/限流/压缩` 等中间件，还支持类式 Controller 组织路由和分层架构，适合从简单 API 到大型全栈项目逐步扩展，不像一些全栈框架那样绑手绑脚。

```python
from litestar import Litestar, get

@get("/")
async def hello_world() -> dict[str, str]:
    """Keeping the tradition alive with hello world."""
    return {"hello": "world"}

app = Litestar(route_handlers=[hello_world])
```

### [Claude Code Game Studios - Claude Code 虚拟游戏开发工作室](https://github.com/Donchitos/Claude-Code-Game-Studios)

一个为 Claude Code 打造的游戏开发多 Agent 协作框架，在单个会话里模拟真实工作室的层级结构：`49 个 AI 代理`分为总监层（Creative/Technical Director）→ 部门主管层（Lead Programmer、Art Director、QA Lead 等）→ 执行专家层（关卡设计、音效、网络、安全等），搭配 `72 个 slash command 工作流技能`（`/brainstorm`、`/design-system`、`/dev-story`、`/code-review`、`/qa-plan` 等），覆盖从脑暴到发布的完整流程。内置 12 个自动校验 Hooks（防硬编码、资产命名规范等）、41 个文档模板（GDD、ADR、Sprint Plan），并提供 `Godot 4`、`Unity`、`Unreal Engine 5` 三套引擎专精代理集，让独立开发者也能享受工作室级的质量把控和协作流程。

```
Tier 1 — Directors (Opus)
  creative-director    technical-director    producer

Tier 2 — Department Leads (Sonnet)
  game-designer        lead-programmer       art-director
  audio-director       narrative-director    qa-lead
  release-manager      localization-lead

Tier 3 — Specialists (Sonnet/Haiku)
  gameplay-programmer  engine-programmer     ai-programmer
  network-programmer   tools-programmer      ui-programmer
  systems-designer     level-designer        economy-designer
  technical-artist     sound-designer        writer
  world-builder        ux-designer           prototyper
  performance-analyst  devops-engineer       analytics-engineer
  security-engineer    qa-tester             accessibility-specialist
  live-ops-designer    community-manager
```

### [键盘按键测试 - 在线键盘检测与连键测试工具](https://keyboard.bmcx.com/)

便民查询网旗下的免费在线键盘测试工具，打开浏览器就能用，无需安装任何软件。页面显示虚拟全键盘布局，按下实体键盘任意按键后对应键位会高亮变色，方便快速定位失灵键或卡键；同时支持 `多键同时按下` 检测键盘的 `无冲（NKRO）` 能力，适合机械键盘换轴后验收或游戏玩家排查组合键冲突。全平台兼容（Windows/macOS/Linux），界面简洁无广告，属于那种「平时用不上、需要时立刻收藏」的实用小工具。

![](https://i.imgur.com/COrLCrP.png)

### [Glance - 屏幕截图即时翻译工具](https://github.com/Harukaon/Glance)

一款主打「截完就译」的屏幕翻译工具，流程极简：框选屏幕区域 → 自动 OCR 识别 → 即时显示译文覆盖在原文上方，无需手动复制粘贴到任何翻译网站。它支持 `100+ 种语言` 互译，内置多种 OCR 引擎适配不同语言文字（含中日韩混排和竖排文字），翻译结果以半透明浮窗形式贴在原位置，不遮挡上下文、可拖拽调整，还支持 `快捷键一键翻译` 和 `多显示器` 场景。基于 Tauri 构建，安装包小巧、内存占用低，适合阅读外语文档、浏览外文网页、查看外文游戏界面等需要「边看边懂」的高频场景。

![](https://i.imgur.com/Q1buqbZ.jpeg)


### [CCX - Claude/Codex/Gemini API 聚合代理](https://github.com/BenedictKing/ccx)

NovelAI 开源的一款 AI API 聚合代理服务，核心价值是用 `一个统一接口` 同时调用 Claude、OpenAI Codex、Google Gemini 等多家大模型的 API，并对上层暴露标准的 OpenAI 兼容格式，已有项目无需改代码即可无缝切换或混用不同模型。它内置了 `智能路由`（按任务类型自动选最合适的模型）、`自动重试与降级`（某模型挂了自动切备用）、`请求缓存`（重复问题直接返回缓存结果）和 `用量统计` 等生产级特性，部署只需一个二进制文件加配置文件，支持 Docker 一键启动，适合需要同时接入多家模型 API 且希望统一管理的团队或个人开发者。

![](https://i.imgur.com/Ew1y0L1.png)

### [ProxyBridge - 开源跨平台透明代理工具](https://github.com/InterceptSuite/ProxyBridge)

一款被广泛称为「Proxifier 究极平替」的开源透明代理工具，能在 `Windows`、`macOS`、`Linux` 三大平台上按进程级拦截并重定向任意应用的 `TCP 和 UDP 流量` 到 HTTP 或 SOCKS5 代理，无需修改目标程序本身。它使用内核级技术（Windows 上是 WinDivert、macOS 用 Network Extension、Linux 用 Netfilter）实现流量劫持，支持按 `进程名/IP/端口/域名` 设置灵活的代理路由规则（代理、直连、阻断），并内置实时流量监控面板方便调试。相比 Proxifier 的商业收费和 Windows 中心化，ProxyBridge 完全免费开源（MIT 协议）、UDP 支持更强、三平台原生 GUI，特别适合让 Steam 游戏、国产软件等不原生支持代理的程序走代理，GitHub 星标已超 5k。

### [imgclip - 跨平台剪贴板图片 CLI 工具](https://github.com/alexyan0431/imgclip)

一款用 Rust 编写的轻量跨平台命令行工具，专门解决「截图后忘记保存就被下次复制覆盖」的痛点。它提供四种工作模式：`单次保存`（把剪贴板图片存为文件）、`watch 监听`（后台持续监控，每张新截图自动保存到指定目录）、`interactive 交互`（逐张提示保存或丢弃）和 `copy 反向`（把本地图片文件复制回剪贴板）。最有用的是 `imgclip --watch --install`，一行命令就能设置开机自启，之后所有截图自动落盘到 `~/Pictures/imgclip/`，完全无感。支持 PNG/JPEG 输出、可调压缩质量、data URI 生成，单二进制文件零依赖，适合开发者、频繁截屏的用户以及需要在脚本或 SSH 环境中处理剪贴板图片的场景。

```bash
$ imgclip --watch
imgclip: saved ~/Pictures/imgclip/imgclip-1746359527000-1.png
imgclip: saved ~/Pictures/imgclip/imgclip-1746359595000-2.png
```

### [MusicBot-Go - 多平台 Telegram 音乐下载机器人](https://github.com/liuran001/MusicBot-Go)

一款用 Go 编写的开源 Telegram 音乐 Bot，采用插件化架构支持网易云、QQ音乐、酷狗、汽水音乐、B站、Apple Music 六大平台的多源搜索与下载。发送歌曲链接或关键词即可获取音乐文件（含封面、歌词、无损音源），内置 `缓存机制`、`限流控制` 和 `并发管理` 保证稳定性，支持 `Inline 查询模式`（`@bot 关键词` 直接搜）和 `听歌识曲`（回复语音消息即可识别）。提供完整版和精简版两种 Docker 镜像，支持动态脚本插件和静态 Go 插件两种扩展方式，可通过 `/settings` 按私聊/群聊维度独立设置默认平台和音质，Docker 一键部署、支持热重载配置，适合自建私有音乐下载服务的用户。

```bash
mkdir -p docker-data
cp config_example.ini docker-data/config.ini
# 编辑 docker-data/config.ini，至少填 BOT_TOKEN

docker run -d --name musicbot-go --restart unless-stopped \
  -w /app/workdir -v "$(pwd)/docker-data:/app/workdir" \
  -e TZ=Asia/Shanghai \
  ghcr.io/liuran001/musicbot-go:latest -c /app/workdir/config.ini
```

> 在线体验：https://t.me/Music163DownBot

### [LX-N Music - 洛雪音乐增强版移动客户端](https://github.com/souvenp/lx-netease-music-mobile)

基于 React Native 开发的开源移动端音乐聚合播放器，在洛雪音乐原版基础上持续改造增强。它以网易云为核心音源（需 Cookie），支持搜索、播放、收藏、MV 播放、每日推荐和 `心动模式`，还提供 `相似歌手/歌曲推荐`、`播放历史自动记录` 和 `批量下载`。最新版本新增 `WebDAV 自动同步`（歌单/设置/音源跨设备同步）、`OneDrive 远程播放`、`Wy 搜索增强`（免费 250 次/月，可补充 Google 搜索）和 `全量导出备份`。界面支持底部抽屉播放列表、圆形旋转封面、桌面歌词和通知栏专辑显示，提供多选歌单操作、评论发送等社交功能，APK 可从 GitHub Releases 直接下载（Apache-2.0 协议），适合需要免费无广告聚合音乐体验的 Android 用户。

### [MIHOMO_YAMLS - Mihomo 每日自动同步配置仓库](https://github.com/HenryChiao/mihomo_yamls)

一个精心整理的 Mihomo（原 Clash Meta）配置文件合集仓库，通过 `GitHub Actions` 每日自动抓取上游优质规则集并生成可直接导入客户端的 YAML 配置，免去手动更新烦恼。仓库按场景严格分类：`General_Config`（通用进阶配置，新手首选）内置分流、去广告、故障转移策略组 15~60+ 个；`Smart_Mode` 专配 SmartDNS 内核适合软路由 7×24 常驻；`Mobile_Modules` 提供 Magisk/KernelSU 透明代理模块；另有 `THEINI`（订阅转换 .ini）和 `THEOPENCLASH`（OpenClash 覆写 .conf）满足不同客户端需求。规则来源透明可查（详见 CREDITS.md），覆盖 AI 服务、流媒体、广告屏蔽、GFW、DMCA 等 20+ 分类，配套三阶段从入门到进阶 Wiki 教程，GitHub 星标超 2.1k，适合想用 Mihomo 但不想从零写配置的用户。

![](https://i.imgur.com/R5cQyqC.jpeg)


### [PPT Master - AI 生成原生可编辑 PPTX 工具](https://github.com/hugohe3/ppt-master)

一款开源 AI 工作流，能将 PDF、Word、Markdown、网页等任意文档转换为 `真正原生可编辑的 PowerPoint 文件`——生成的每个文本框、形状、图表都是 DrawingML 原生元素，可在 PowerPoint 中直接点击修改、调整布局和套用模板，而非市面常见的「一页一张大图」式死板输出。它支持 `原生幻灯片转场` 和 `对象入场动画`，自动生成每页演讲者备注并可合成音频旁白实现「自读」演示，内置多种专业设计风格（杂志风、瑞士栅格、数据新闻等）。项目以 Claude Opus + AI 生图为推荐模型组合，本地运行数据隐私安全，适合需要从长文、报告、论文快速生成可继续编辑的演示文稿的用户，GitHub 星标已超 2.8 万。




### [Monica Pass - 本地优先开源密码管理器](https://github.com/JoyinJoester/Monica)

一款本地优先的开源密码管理器，核心亮点是同时聚合 `Bitwarden`（API 同步生态）和 `KeePass`（.kdbx 本地文件）两套密码生态，支持直接导入两家的数据而无缝切换。它以 `AES-256-GCM` 加密存储登录凭证、银行卡、身份信息和私密笔记，内置 `TOTP 动态验证码生成器`（支持二维码扫描导入）实现密码与 2FA 一站管理，提供生物识别解锁、Passkey 支持（Android 14+）和浏览器扩展自动填充。数据可选 `WebDAV` 自建同步实现跨设备流转，项目活跃迭代频繁，GPL-3.0 协议完全开源，适合追求隐私自主、不想依赖商业云端密码管理服务的用户。

![](https://i.imgur.com/sWv5hlm.png)

### [GenericAgent - 极简自进化 AI Agent 框架](https://github.com/lsdefine/GenericAgent)

一个仅约 `3.3K 行种子代码` 的自进化自主 Agent 框架，核心理念是「不要预加载技能，让它自行进化」——每次完成任务后自动将执行路径「晶化」为可复用 `Skill`，长期使用形成个人专属技能树。Agent Loop 仅约 100 行，提供 9 个原子工具（代码执行、文件读写、网页感知等）覆盖 `终端/文件系统/浏览器/键鼠/屏幕视觉/ADB 移动设备` 全系统控制。通过 `五层分层记忆系统`（L0 元规则到 L4 会话归档）实现上下文窗口始终保持在 30K token 以内，比同类 Agent 省 6 倍 token、成功率更高、幻觉更少。支持 Claude、Gemini、Kimi 等主流模型，提供 TUI/Streamlit/Telegram/Discord 等多前端，GitHub 星标超 12.9k，适合追求极简架构和长期自动化能力增长的开发者。

![](https://github.com/lsdefine/GenericAgent/raw/main/assets/demo/discord_hcaptcha_real_browser.gif)

### [Markdown to Text Converter - 在线 Markdown 去格式转换器](https://markdowntotext.com/)

一款免费的在线 Markdown 转纯文本工具，功能专一且高效：粘贴 Markdown 内容后一键剥离所有格式标记（标题符号、粗体斜体、链接语法、图片引用、代码块、列表缩进等），输出干净的纯文本。全部转换在浏览器本地完成，不上传任何数据到服务器，支持 `一键复制` 和 `下载 PDF` 输出，也可选择转为 Rich Text 或 HTML 格式。界面简洁、即开即用、无需注册，适合需要从 Markdown 文档中快速提取纯文字内容的场景，如粘贴到不支持富文本的表单、发纯文本邮件、做内容预览等。

![](https://i.imgur.com/vO7yTlx.png)

### [ArgoSBX - 甬哥一键代理节点部署脚本](https://yonggekkk.github.io/argosbx/)

甬哥（ygkkk）开发的「小钢炮」一键代理部署方案，通过网页端 `一键 SSH 命令生成器` 勾选协议和参数即可生成完整部署命令，粘贴到 VPS 运行即可完成节点搭建。脚本以 `Sing-box + Xray + Cloudflared-Argo` 三内核自动分配为核心，支持 `VLESS-Reality`、`Hysteria2`（端口跳跃）、`AnyTLS`、`Shadowsocks-2022` 等多种协议自由组合，内置 15 种 `WARP 出站` 组合可快速更换落地 IP 解锁流媒体。最大的特色是 `Argo Tunnel` 集成——即使 VPS IP 被封或端口受阻，通过 Cloudflare 隧道依然可正常使用，支持 Docker 部署并提供一键升级回填命令，适合需要快速、低成本搭建稳定代理节点的用户。

![](https://i.imgur.com/ZxyZ8PU.png)


### [Sub2API - AI 订阅一站式开源中转服务](https://github.com/Wei-Shaw/sub2api)

一款将 Claude、OpenAI、Gemini、Antigravity 等多家 AI 服务的网页订阅账号转化为标准 API 接口的开源中转平台，实现订阅配额的统一接入、共享和成本分摊。它支持 `多账号池管理`、`智能负载均衡`（Sticky Session 会话粘性、故障转移）和 `Token 级精确计费`，内置支付系统（支付宝/微信/EasyPay/Stripe）让用户自助充值，团队或好友可通过「拼车」共享高价订阅。提供 Web 管理后台统一监控用量、账号和用户，原生兼容 `Claude Code`、`Codex CLI`、`Cursor`、`Gemini CLI` 等工具无需修改客户端配置，支持一键脚本安装和 Docker Compose 部署（Go + Vue 3 + PostgreSQL + Redis），GitHub 星标超 2.8 万。

![](https://i.imgur.com/8q3pzUT.png)

### [VibeProxy - macOS 菜单栏 AI 订阅代理](https://github.com/automazeio/vibeproxy)

一款原生 macOS 菜单栏应用（SwiftUI 构建），核心理念是「Stop paying twice for AI」——让你已有的 `Claude Code`、`ChatGPT/Codex`、`Gemini`、`Kimi`、`Qwen`、`Antigravity` 等订阅通过本地代理服务器（默认端口 8317）直接服务于各种 AI 编码工具，无需额外购买 API Keys。支持 `OAuth 一键登录` 多个平台、`多账号 round-robin 轮换` 和 `auto-failover` 自动故障转移避免快速触达限额，兼容 Factory Droids、Amp Code、Zed 等 OpenAI 兼容工具。菜单栏图标一键启停代理、实时状态监控，完全本地运行隐私友好，MIT 协议开源，仅限 Apple Silicon Mac，适合同时持有多个 AI 订阅且想在编码工具中统一调用的开发者。

![](https://i.imgur.com/hQHoZUt.png)

### [ShotX - 现代 macOS 菜单栏截图工具](https://github.com/aimen08/shotx)

一款用 Swift 原生开发的轻量 macOS 截图与录屏工具，常驻菜单栏、体积仅约 1.6 MB，完全免费开源无订阅。它支持 `区域/窗口/全屏截图`、自定时器（3/5/10 秒）和从剪贴板打开图片编辑，录屏使用 `ScreenCaptureKit` 输出 H.264 MP4 或 GIF（720p），支持 `麦克风和系统音频` 录制、鼠标点击高亮以及 `Webcam 叠加`（录制时画面左下角显示摄像头自视图）。内置标注工具（箭头、矩形、文本、编号步骤）、`Apple Vision OCR 文字提取`、原生 `取色器`（HEX 自动复制）和最近 100 条捕获历史，全局快捷键高度可配置且实时生效无需重启，支持 Sparkle 自动更新，macOS 13.0+ 可用，Homebrew 安装一行搞定。

![](https://i.imgur.com/rZqBVYE.png)


### [Snapzy - 开源 macOS 截屏录屏神器](https://github.com/duongductrong/Snapzy)

一款 Swift + SwiftUI 构建的开源 macOS 原生截屏与录屏应用，被誉为 CleanShot X 的最佳免费替代品，GitHub 星标超 1.4k。它在截图方面提供 `区域/窗口/全屏/滚动截图`（带实时拼接预览）、`OCR 文字提取`（多语言高准确率）和 `对象抠图`（自动移除背景透明化），标注编辑器支持形状、箭头、文本、水印、模糊像素化、`自动敏感数据脱敏`、3D 模拟背景和视频编辑器（时间线裁剪、Follow Mouse 缩放）。录屏支持视频/GIF 输出、系统音频 + 麦克风、`按键叠加显示` 和实时屏幕标注。云端上传支持自带 AWS S3 或 `Cloudflare R2`（凭证存 Keychain、无第三方服务器），提供可配置全局快捷键、多语言（含简中）和 URL Scheme 自动化集成，BSD-3 协议开源，适合追求 CleanShot X 级功能但不想付费订阅的用户。

![](https://i.imgur.com/SmSjW9p.jpeg)


### [App Store Price - 全球 App Store 价格查询与追踪](https://appstoreprice.org/zh/apps)

一款实时全球 App Store 价格对比工具，覆盖 `iOS`、`iPadOS` 和 `Mac` 应用的当前价格、历史走势和跨区最低价查询。支持搜索任意应用查看全球各区的定价（自动汇率换算为 USD/CNY/JPY 等），详情页可查看 `应用内订阅价格` 和 `历史变化记录`（每天自动更新），帮助用户找到全球最低价区节省开支。首页展示热门应用的全球最低价示例，提供价格变化通知功能追踪最佳购买时机，支持多格式导出和货币切换，全部数据实时抓取而非静态列表，适合在多区 App Store 间比价购买应用、订阅或内购的用户。

![](https://i.imgur.com/7HnxC0M.png)


### [DBX - 15MB 开源跨平台数据库客户端](https://github.com/t8y2/dbx)

一款用 Tauri（Rust + Vue）构建的开源轻量级数据库管理工具，安装包仅约 `15MB`，无 Java JRE、Python 或捆绑 Chromium 等运行时依赖，启动快、占用低。原生支持 `MySQL`、`PostgreSQL`、`SQLite`、`Redis`、`MongoDB`、`DuckDB`、`ClickHouse`、`SQL Server` 等 50+ 种数据库，通过 Agent/JDBC 还可扩展 Oracle、Elasticsearch、Snowflake 等。查询编辑器基于 CodeMirror 6 提供 SQL 语法高亮和元数据感知补全，内置 `AI SQL 助手`（自然语言生成 SQL，兼容 Claude/OpenAI/Ollama）和 `MCP Server`（让 Claude Code/Cursor 等直接查询数据库）。数据网格支持虚拟滚动、行内编辑和导出（CSV/JSON/Markdown/XLSX），Redis 浏览器支持所有数据类型的键操作，提供 ER 关系图、Schema Diff、SSH 隧道、加密配置导入导出和 Docker 自托管 Web 版，适合需要轻量多数据库统一管理的开发者。

![](https://i.imgur.com/OMahjSt.png)

### [DeepSeek-TUI - 终端原生 AI 编码代理](https://github.com/Hmbown/DeepSeek-TUI)

一款用 Rust 编写的开源终端 AI 编码代理（现更名 CodeWhale），专为 `DeepSeek V4` 等模型优化，支持 25+ 个模型 Provider（DeepSeek、OpenRouter、Ollama、Claude、OpenAI 等）。核心设计是三种工作模式：`Plan`（只读分析不修改文件）、`Agent`（每步操作需审批）、`YOLO`（全自动执行），支持 1M token 上下文、`Side-Git 快照回滚`（不影响主仓库历史）、`OS 级沙箱`（macOS Seatbelt、Linux Landlock）和持久会话。可读写文件、执行 Shell、Git 管理、`LSP 诊断`、`网页搜索`，支持 `RLM 并行子代理`（最多 16 个廉价模型实例扇出处理复杂任务）和 `MCP 客户端/服务端`扩展。终端原生 TUI 界面流式展示思考过程，支持无头 CLI 模式用于脚本/CI，MIT 协议开源，一行 npm install 即可使用，被称为「DeepSeek 版 Claude Code」。

![](https://i.imgur.com/ZbjtZdd.png)

### [Warp - Agent 化开发环境](https://github.com/warpdotdev/warp)

一款从现代终端演进而来的 `Agentic Development Environment`，用 Rust 编写（客户端约 98% Rust）、GPU 加速渲染，2026 年客户端完全开源。它不仅提供块式输出、命令搜索、智能补全、语法高亮等现代终端体验，更内置 `Warp Agent/Oz` 编排代理——在 Terminal-Bench 排名第一、SWE-bench Verified 达 71%，支持 `自然语言驱动` 的多步任务执行（建项目、写代码、调试、Git 操作），可同时运行 `Warp Agent + Claude Code + Codex + Gemini CLI` 等多个代理并统一管理。内建代码编辑、Git diff 审查、commit 工作流，全程人类主导可随时干预回滚，支持 macOS/Windows/Linux，适合将终端作为核心工作环境且需要 AI 代理深度集成的全栈开发者。

![](https://i.imgur.com/mCc5n5I.png)

### [CC GUI - Claude Code 图形化 VibeCoding 平台](https://github.com/zhukunpenglinyutong/desktop-cc-gui)

一款将 `Claude Code`、`Codex CLI`、`OpenCode` 等命令行 AI 编程工具封装为图形界面的开源桌面客户端，基于 `Tauri 2 + React 19 + TypeScript` 构建，支持 macOS/Windows/Linux 跨平台。聊天界面支持 `@文件引用`、图片/语音输入、斜杠命令和 `AI 操作透明化`（文件修改、Shell 命令以卡片实时展示），可切换或混用不同 AI 引擎。集成开发面板包含 `文件树`、`内置终端` 和 `Git 智能面板`（AI 生成 commit message、diff 查看），项目管理提供 `Plan 面板`、`Kanban 看板`、`项目知识地图`（AI 扫描生成代码关系图谱）和 `上下文账本`（token/费用/预算可视化）。支持 `MCP/Skills/插件市场` 一键扩展 AI 能力、15+ 主题和中英双语，GitHub 星标超 3k，适合喜欢可视化操作 Vibe Coding 工作流的开发者。

![](https://i.imgur.com/h2oFEnf.png)

### [CodexMonitor - OpenAI Codex 多代理监控中心](https://github.com/Dimillian/CodexMonitor)

一款由 OpenAI 开发者 Dimillian 制作的跨平台桌面应用（Tauri + React），专门用于 `同时编排和管理多个 OpenAI Codex 代理`。它提供多 `工作空间`（每个独立运行一个 Codex app-server）、`线程管理`（恢复、钉住、归档、草稿、中断）和 `Home Dashboard` 快速跳转最近活动，Composer 支持图片附件、语音输入、技能/提示词/文件路径自动补全。内置 `Git & GitHub 深度集成`（diff 统计、暂存/撤销、Issues/PR 查看）和核心的 `OpenAI 使用额度监控`——侧边栏实时显示账户 rate limits 和 credits 余额、使用快照仪表盘。支持 `远程后端 daemon 模式`（可通过 Tailscale 从 iOS 远程监控桌面代理活动），MIT 协议开源，适合重度 Codex 多项目用户，被誉为「从多个 VS Code 到一个应用」的效率利器。

![](https://i.imgur.com/Ykq9q4u.png)


### [Mux0 - macOS 原生 AI Agent 终端](https://mux0.com/)

一款专为 AI 编码代理优化的 macOS 原生终端应用，基于 `libghostty` 引擎（Ghostty 终端核心）和 Metal GPU 渲染，由开发者 10xChengTu 打造。核心亮点是 `AI Agent 实时状态监控`——侧边栏和标签页图标直观显示 Claude Code、OpenCode、Codex 等代理的运行状态（运行中、空闲、等待输入、成功、失败），悬停可查看当前工具和最后回复摘要，通过本地 Unix Socket 实现无需网络和账号。采用 `Workspace → Tab → Split` 三层项目导向结构，侧边栏显示每个工作区的 `Git 分支`、开放 PR 和未读通知，布局跨重启持久化保留。内置所有 Ghostty 主题、支持背景透明度和毛玻璃模糊，中英双语界面即时切换，纯原生 App 无 Electron 依赖，macOS 14.0+ 可用。

![](https://i.imgur.com/zENJvct.png)

### [Aibijia - AI 订阅一键比价平台](https://aibijia.org/)

一款专注 AI 订阅服务的中文一键比价平台，由开发者卡皮巴拉（ka-pi-ba-la）维护，GitHub 星标超 1.3k。它聚合多个卡网、中转商和代充渠道的实时价格，覆盖 `ChatGPT`（Plus/Pro 5x 20x/Team/Business）、`Claude`（Pro/Max 5x 20x/Team）、`Gemini`（Pro/Ultra）、`Grok`（Super Grok）等主流服务，支持按模型/套餐筛选、搜索和价格范围设置。网站定期更新行情，还提供低价区拼车攻略（如菲律宾区 ChatGPT Pro）和风控避坑讨论，配套有社区论坛（forum.aibijia.org）供用户交流，适合追求性价比的 AI 订阅用户快速找到最便宜的购买渠道。

![](https://i.imgur.com/uq14jnC.jpeg)

### [Free Claude Code - 免费使用 Claude Code 的本地代理](https://github.com/Alishahryar1/free-claude-code)

一款轻量级本地代理服务器（Python FastAPI 实现），让 `Claude Code` 和 `Codex` 无需 Anthropic 付费订阅即可使用，GitHub 星标数万。核心原理是拦截客户端的 API 请求并路由到用户配置的任意后端 LLM——支持 `NVIDIA NIM`（免费 40 req/min）、`OpenRouter` 免费模型、`Ollama`/`LM Studio` 等本地模型，也可 per-model 分别路由不同后端。通过 `fcc-claude` 和 `fcc-codex` 启动器自动注入环境变量，兼容 `终端 CLI`、`VSCode 扩展` 和 `Discord/Telegram Bot`（可选集成，支持语音转录），本地 Admin UI 一键配置 API Key 和模型映射，MIT 协议开源，适合想零成本或完全离线使用 Claude Code 能力的开发者。

![](https://github.com/Alishahryar1/free-claude-code/raw/main/assets/pic.png)

### [Huashu Design - Claude Code 原生 HTML 设计 Skill](https://github.com/alchaincyf/huashu-design)

一款开源的 HTML-native 设计 Skill，专为 Claude Code 及其他 AI 编码 Agent 打造，由开发者花叔（@AlchainHust）创建，GitHub 星标超 1.8 万。通过一句话 Prompt 即可生成 `高保真交互原型`、`可编辑 PPTX 幻灯片`、`Motion 动画`（支持 MP4/GIF 导出、25/60fps 插帧）和 `印刷级信息图`，全部输出为单文件 HTML 或可直接编辑的 PPTX。内置 `20 种设计哲学` + `40 种 HTML 原生风格库`（纯 CSS 无需生图），`5 维专家评审`（哲学一致性/视觉层级/细节执行/功能性/创新性自动打分迭代），支持品牌资产自动适配（logo、色板提取），模糊需求时自动并行生成 3 套视觉方向供选择。`npx skills add alchaincyf/huashu-design` 即可安装，跨 Agent 通用，MIT 协议开源。

![](https://github.com/alchaincyf/huashu-design/releases/download/v2.0/hero-animation-v10-en.gif)

### [OpenACP - 自托管 AI 编码代理聊天桥接](https://github.com/Open-ACP/OpenACP)

一款开源的自托管桥接工具，让用户通过 `Telegram`、`Discord` 或 `Slack` 等即时消息平台与 AI 编码代理实时交互，基于 `Agent Client Protocol (ACP)` 开放标准构建。支持 `Claude Code`、`Codex`、`Gemini CLI`、`Cursor` 等 28+ 个代理，在聊天中发送消息即可触发代理读取代码库、修改文件、执行命令，并将思考过程、工具调用和权限请求实时流式回传到聊天界面。完全本地运行、使用自己的 API 密钥、无需云端中继，支持 `语音消息转录`（Groq Whisper）、`会话成本跟踪`、`预算限制` 和端口动态暴露，`npm install -g @openacp/cli` 快速启动，MIT 协议开源，适合从手机或聊天工具远程控制本地 AI 编码代理。

![](https://i.imgur.com/LQPQRO3.jpeg)

### [Tolaria - Markdown 知识库桌面管理器](https://github.com/refactoringhq/tolaria)

一款免费开源的跨平台桌面应用（Tauri + React + TypeScript），专为管理基于 Markdown 的知识库设计，由 Refactoring.fm 作者 Luca Rossi 开发。核心原则是 `文件优先`、`Git 优先`、`离线优先`——笔记就是纯 Markdown + YAML frontmatter，每个 Vault 就是一个 Git 仓库。内置 `Git 客户端`（commit、push、历史浏览）、富文本块式编辑器（slash 命令、wikilinks 双向链接、白板、媒体预览）、`Inbox`、`Types`（自定义笔记类型带图标颜色）、`Favorites`、自定义 Views 和关系图谱。强调 `AI 友好`——通过 AGENTS.md 文件让 Claude Code 等代理直接读写整个知识库，作者本人用它管理 10,000+ 笔记，GitHub 星标数万，AGPL 协议开源，适合追求数据主权和 AI 集成的知识工作者。

![](https://i.imgur.com/jchseug.png)

### [Discourse Saver - 通用 Discourse 论坛内容保存工具](https://github.com/acheng-byte/discourse-saver)

一款开源的浏览器扩展和油猴脚本，专为 Discourse 论坛用户设计，支持一键将帖子（主帖 + 评论）保存到多种笔记平台。兼容全球数百个 Discourse 站点（如 Linux.do、Rust Users、Cloudflare Community 等），`Raw Edition` 直接使用 Discourse 原生 Markdown 实现 `表格/代码块零损耗转换`。支持保存到 `Obsidian`（Markdown 本地文件）、`飞书多维表格`、`Notion Database`、`语雀`、`思源笔记` 或导出为增强型 HTML（含 Lightbox、表格优化、PWA），图片可 Base64 嵌入或压缩，评论支持批量 API 获取和折叠显示。页面右侧悬浮保存按钮，单击保存主帖、长按弹出菜单选择楼层，`Ctrl+Shift+S` 快捷键一键操作，MIT 协议开源，适合重度 Discourse 用户做知识存档。



### [Obsidian Web Clipper - Obsidian 官方网页剪藏扩展](https://github.com/obsidianmd/obsidian-clipper)

Obsidian 团队官方出品的浏览器扩展，支持 `Chrome`、`Firefox`、`Safari`、`Edge` 等主流浏览器，将网页内容高亮捕获并保存为 `持久化 Markdown 文件` 直接送入 Obsidian Vault。支持全页保存或仅选中文本，自动提取元数据（标题、URL、作者、日期），内置多种预设模板（文章、食谱、参考文献、学术论文等）并支持 `社区自定义模板`。基于 `Defuddle` 引擎进行内容提取，保留干净可读的 Markdown 格式，高级功能包括 `AI 数据解释`（自然语言提取摘要，支持本地模型）、Schema.org/OpenGraph 元数据提取、CSS 选择器过滤和规则引擎。Chrome Web Store 评分 4.8、用户超 80 万，完全本地处理无数据上传，MIT 协议开源，GitHub 星标 4.6k。

![](https://i.imgur.com/uNB74Vr.png)

### [Claude Context - 代码语义搜索 MCP 服务器](https://github.com/zilliztech/claude-context)

一款由 Zilliz 开发的开源 MCP 服务器，为 Claude Code 及其他 AI 编码代理提供 `语义代码搜索` 能力，解决大型代码库直接加载上下文导致 token 爆炸的问题。采用 `BM25 + 稠密向量混合检索`，基于 `AST 感知分块`（支持 14+ 编程语言）将代码库索引到 `Milvus/Zilliz Cloud` 向量数据库，查询时仅注入相关代码片段。基准测试显示可减少约 `39.4% token 用量` 和 `36.1% 工具调用`，支持增量索引、差分更新。暴露 `index_codebase`、`search_code`、`clear_index`、`get_indexing_status` 四个 MCP 工具，可完全本地运行（`Milvus Lite` + Ollama embedding），MIT 协议开源，GitHub 星标超万，适合中大型项目的 Claude Code/Cursor 用户。

![](https://camo.githubusercontent.)

### [YTLite - iOS YouTube 原生增强器](https://github.com/dayanch96/YTLite)

一款由开发者 dayanch96 打造的 iOS YouTube 原生增强工具（现更名 YouTube Plus），通过注入自定义功能到官方 YouTube App 实现超百项可自定义选项。核心功能包括 `无广告播放`、`后台播放`、`视频/音频下载`（支持音轨选择）、`SponsorBlock` 自动跳过赞助片段、`手势控制`、`默认画质/速度设置`、`PiP 画中画`、`120Hz` 支持、`OLED 暗黑模式`、`Return YouTube Dislikes` 和 `高清解锁`（配合 YTUHD）。支持隐藏 Shorts、重新排序标签栏、睡眠定时器等界面定制，可通过越狱 .deb 直接安装或 TrollStore/Sideload 侧载 IPA 构建，从 v5.2 起转为 Patreon 订阅模式（最低约 1 美元/月），适合想在 iOS 上获得类 Premium 体验的 YouTube 重度用户。

![](https://i.imgur.com/DmCv0vE.png)

### [Zotero PDF2Zh - Zotero PDF 中文翻译插件](https://github.com/guaguastandup/zotero-pdf2zh)

一款基于 PDF2zh 项目的 Zotero 开源 PDF 中文翻译插件，专为科研人员阅读英文学术论文打造。在 Zotero 中选中 PDF 附件一键调用后端服务翻译，支持 `纯中文版`、`中英对照版` 等多种输出样式，能智能 `保留数学公式`、`表格`、`图表` 和原排版，支持 `双栏论文转单栏` 便于手机阅读。兼容 `DeepSeek`、`GLM-4`、`Bing`、`Ollama` 等多种翻译引擎，支持自定义 Prompt 和术语表提高学术翻译准确性，可指定中文字体（如霞鹜文楷）。采用客户端-服务器架构，后端可 Python 脚本或 Docker 部署，AGPLv3 协议开源，GitHub 星标超 4k，适合使用 Zotero 管理文献并需要快速翻译英文 PDF 的中国科研人员和学生。

![](https://i.imgur.com/g38Gxgy.png)

### [Rebased - JetBrains 风格开源 Git 客户端](https://github.com/DetachHead/rebased)

一款基于 IntelliJ 平台的开源 Git 图形化客户端（fork 自 IntelliJ Community Edition），由开发者 DetachHead 打造，被誉为「目前最强的 Git 图形化客户端」。它继承了 `JetBrains 顶级的语法级 Diff 查看`、`交互式 Rebase 编辑器`（拖拽重排）、`视觉化冲突解决器`、`Hunk 暂存`、`Changelists`、`Git Log 图表`、`本地历史` 等完整 VCS 工具窗，同时移除了除 Git 集成外的几乎所有插件保持轻量。独家支持 `禁用 .idea 目录创建`（兼容纯 VS Code 项目）、`Git Worktree` 原生支持、多 Tab 内置终端和全文搜索。快捷键和操作流 100% 兼容 JetBrains 生态，Apache 2.0 协议开源，GitHub 星标 4.2k，支持 Windows/macOS/Linux（winget/brew/AppImage 安装），适合习惯 JetBrains Git 体验但不想打开完整 IDE 的用户。

![](https://i.imgur.com/ERi4sUQ.png)

### [Andrej Karpathy Skills - Claude Code 行为优化指南](https://github.com/forrestchang/andrej-karpathy-skills)

一个极简但广受欢迎的 GitHub 仓库，包含单一 `CLAUDE.md` 文件，将 Andrej Karpathy 对 LLM 编码常见陷阱的观察提炼为四条实用行为准则：`Think Before Coding`（不随意假设、遇到困惑先提问）、`Simplicity First`（只写最小必要代码、避免过度工程化）、`Surgical Changes`（只改必须改的部分、不顺手重构无关代码）和 `Goal-Driven Execution`（将任务转化为可验证的成功标准、先写测试再实现）。直接下载放入项目根目录即可生效，Claude Code 自动读取，也兼容 Cursor 等工具，GitHub 星标数万。这四条原则能有效减少 AI 编码时的幻觉、过度复杂化和不必要修改，被开发者广泛用于各类项目中。

```
/plugin marketplace add forrestchang/andrej-karpathy-skills
```

### [BYOKEY - AI 订阅转标准 API 网关](https://github.com/AprilNEA/BYOKEY)

一款用 Rust 编写的开源本地 AI API 网关，全称 Bring Your Own Keys，能将 `Claude Pro`、`OpenAI Plus`、`GitHub Copilot` 等订阅凭证转换为标准的 `OpenAI/Anthropic 兼容 API 端点`。自动处理 `OAuth 登录`（PKCE、device-code），Token 持久化存储到 SQLite，也支持直接配置原始 API Key 跳过 OAuth。覆盖 Claude、Codex/OpenAI、Copilot、Gemini、Qwen、Kimi 等 10+ 提供商，`byokey login claude` → `byokey serve` 两步启动本地代理（端口 8018），在 Cursor、Amp Code、Factory CLI 等工具中只需修改 `base_url` 即可使用，支持热重载配置和 systemd/launchd 后台服务，MIT 协议开源，适合同时持有多个 AI 订阅并想在编码工具中统一调用的开发者。

![](https://i.imgur.com/cp5ftl5.png)

### [Portapps - Windows 开源便携应用集合](https://portapps.io/)

一个免费开源的 Windows 便携应用打包项目，由开发者 CrazyMax 维护，通过 GoLang 编写的包装器将流行桌面软件打包为无需安装的 `便携版本`。支持 `Android Studio`、`Brave`、`Discord`、`DBeaver`、`VS Code`、`VLC`、`Wireshark`、`IntelliJ IDEA Community`、`Cryptomator` 等 54+ 款应用，提供便携安装包和 7z 压缩两种格式。每个应用通过独立 `-portable.exe` 启动，负责路径重定向和配置隔离，数据和设置保存在应用目录内、不修改系统注册表，可从 USB 闪存盘或云文件夹直接运行。整个构建过程在 GitHub Actions 中公开透明，支持 YAML 配置文件自定义参数，MIT 协议开源，适合需要在多台电脑间随身携带开发环境或受限环境下免安装使用软件的用户。

![](https://i.imgur.com/hfogy5a.png)

### [tlrc - tldr 官方 Rust 客户端](https://github.com/tldr-pages/tlrc)

tldr-pages 组织官方维护的 tldr 命令行帮助工具客户端，用 Rust 编写，定位为推荐的统一官方实现。tldr（Too Long; Didn't Read）提供简洁、示例驱动的命令帮助页面，作为传统 `man` 手册的友好替代——输入 `tldr tar` 或 `tldr git checkout` 即可看到常用用法示例，而非翻阅冗长的 man page。tlrc 严格遵循 `tldr 客户端规范`，自动管理本地页面缓存（默认最长 2 周），支持多语言和自定义颜色/格式。安装方便：`brew install tlrc`（macOS）、`cargo install tlrc`、`winget install tldr-pages.tlrc`（Windows），配置文件 `~/.config/tlrc/config.toml`，MIT 协议开源，GitHub 星标 950+，适合日常命令行快速查阅工具用法的开发者。

![](https://i.imgur.com/H6CKJt4.png)

### [Dokobot - AI Agent 网页浏览工具](https://dokobot.ai/)

一款专为 AI 代理设计的免费网页浏览工具，核心思路是让 AI 通过用户的 `真实 Chrome 浏览器` 读取任意网页内容，而非依赖云浏览器或爬虫。以 Chrome 扩展形式运行（支持 Chrome/Edge/Brave/Arc），能处理 `JavaScript 渲染的 SPA`、`登录态页面`、`无限滚动` 和 `反爬虫机制`（如 Cloudflare），输出 LLM 友好的 `结构化 Markdown`，据称最高可节省 99% Token。完全本地运行无需注册、无 API Key、无限免费，不共享密码或使用云端浏览器。支持 CLI（`npm i -g @dokobot/cli`）、MCP 和 Skill 集成（可接入 Claude Code、Cursor 等代理），Chrome Web Store 评分 4.6、用户超 5000，适合需要让 AI 代理读取真实网页内容的研究和数据提取场景。

![](https://i.imgur.com/4JzJFgQ.png)

### [CRACKSurl - 综合软件资源下载站](https://cracksurl.com/)

一个运营超过 10 年的软件资源下载站，自称「The Ultimate Software Hub」，每日更新 Windows 和 Android 平台的软件资源。2012 年从 Blogger 起步，经历多次域名迁移后稳定运营至今，在 Reddit r/Piracy 社区 Megathread 中被评为「GOAT」。内容涵盖 `Office 办公套件`、`下载工具`（IDM 等）、`音视频处理`、`数据恢复`、`系统工具` 和 `Android 修改版应用` 等，不直接托管文件而是链接到 Upload.ee 等第三方文件托管平台，提供详细的下载步骤指引。网站设有 DMCA 响应机制和免责声明，建议使用 `AdBlocker` 屏蔽广告，适合需要获取各类软件资源的用户。

![](https://i.imgur.com/q9h4ddE.png)

### [IDEA-Set - JetBrains IDE 配置与工具合集](https://github.com/zpj80231/idea-set)

一个由开发者 zpj80231 维护的 GitHub 仓库，GitHub 星标超 1.1k，系统整理了 `JetBrains IntelliJ IDEA` 及全家桶的个人自用配置、常用插件推荐和工具资源。配置部分涵盖 `主题/字体/编码` 设置、`Live Templates` 代码模板、文件头注释等个性化配置，支持通过 JetBrains 账号或 GitHub Settings Repository 远程同步。插件推荐包含 `Lombok`、`Rainbow Brackets`、`MyBatisCodeHelper-Pro`、`Maven Helper`、`Translation`、`Key Promoter X` 等实用选择，附带优化建议（关闭不必要插件减少内存占用）。仓库还提供 `VM Options 配置`、`ja-netfilter` 等全家桶工具，附 DISCLAIMER 免责声明，适合想快速搭建优化 IDEA 环境的 Java/Kotlin 开发者。

### [Claude Code HowTo - 可视化示例驱动进阶指南](https://claude-howto.vercel.app/)

一份以可视化示例为核心的 Claude Code 进阶使用指南，覆盖从基础操作到高级工作流的全链路最佳实践。每个知识点都配有 `实时动图演示` 或交互式代码片段，让用户直观看到 Claude Code 在真实项目中的操作路径——比如多文件重构、测试驱动开发、Git 工作流自动化等场景的完整操作录像。指南按照从入门到精通的难度递进编排，包含 `Hooks 自动化`、`MCP 工具集成`、`Sub Agent 并行` 和 `上下文管理策略` 等核心主题，每个章节末尾附有可直接复制使用的 `Prompt 模板` 和 `CLAUDE.md 配置示例`，适合已经上手 Claude Code 但想系统提升使用效率的开发者。

![](https://i.imgur.com/PY1G0Ui.png)


### [CodexDesktop Rebuild - OpenAI Codex 跨平台桌面重构](https://github.com/Haleclipse/CodexDesktop-Rebuild)

由开发者 Haleclipse（Cometix Space）维护的 OpenAI Codex 桌面客户端跨平台重构版本，基于 `Electron Forge` 构建，解决了官方 Codex Desktop App 仅支持 macOS 和 Windows 的问题，额外提供 `Linux`（x64/arm64）原生安装包。它完整保留了官方 Codex 的核心体验，包括 `多线程并行工作`、`多项目切换`、`Git worktree 隔离`、`内置 diff 视图和终端`、`Computer Use 桌面操控`、`语音输入`、`内置浏览器预览`、`Appshots 应用截图` 以及 `Skills/插件/MCP 扩展` 等功能。用户登录 OpenAI 账号即可使用，支持创建隔离的 Git worktree 进行并行开发，自动化任务运行在专用沙箱中，还提供 `浮动弹出窗口` 可将活动线程独立置顶。通过 GitHub Actions 自动构建各平台安装包，持续跟进官方 Codex 版本更新，GitHub 星标约 2.4k，适合需要在 Linux 上使用 Codex 或希望获得跨平台一致体验的开发者。

![](https://i.imgur.com/IxReg3M.png)

### [SpotX - Spotify 桌面端去广告脚本](https://github.com/amd64fox/SpotX)

一个针对 Spotify 桌面端的去广告和功能增强脚本，覆盖 Windows、macOS 和 Linux 三大平台。运行脚本后可以 `屏蔽所有音频广告和横幅广告`、`解锁跳过按钮`（包括强制播放的播客广告）、`启用隐藏功能`（如歌曲淡入淡出、客户端实验性功能）和 `移除升级提示`。脚本支持 `Bash 一键安装` 和手动安装两种方式，提供多种参数选项自定义需要开启或关闭的功能，还内置 `BlockTheSpot` 集成支持在 Windows 上更彻底地屏蔽广告元素。项目持续更新适配 Spotify 最新版本，适合受够了免费版 Spotify 频繁广告打断体验的用户。Mac Apple Silicon 用户可使用社区维护的 [BlockTheSpot-Mac-Arm](https://github.com/mucsbr/BlockTheSpot-Mac-Arm) 分支获得更好的 ARM 原生支持。



### [Type4Me - macOS 智能语音输入工具](https://github.com/joewongjc/type4me)

一款由开发者 joewongjc 打造的开源 macOS 菜单栏语音输入工具，被视为 Typeless 的免费开源平替。核心亮点是 `双引擎语音识别`——本地版内置 `SenseVoice`（流式实时识别，边说边出字）+ `Qwen3-ASR`（最终校准提升准确率），云端版支持火山/豆包、OpenAI、Deepgram 等 8+ 家厂商切换。识别后可通过 `大模型后处理` 进一步优化输出，内置 `快速模式`（零延迟注入）、`语音润色`（消除口头语、优化表达）、`英文翻译`、`Prompt 优化` 和自定义模式，支持 13+ 家 LLM 提供商。还提供 `热词管理` 和 `片段映射`（如语音说「我的邮箱」自动替换为真实地址），搭配官方 `type4me-vocab-skill` 可让 Claude Code 自动管理词汇表实现接近 100% 准确率。所有数据完全本地存储，支持历史记录 CSV 导出，macOS 14+ 可用，GitHub 星标超 1.2k，适合追求隐私安全和高度可定制的语音输入重度用户。

![](https://i.imgur.com/n7cJbrX.png)

### [Kitty 猫脚本 - 专注于颜色网站浏览器脚本](https://kittymao.xyz/)

懂得都懂，请自行测试

![](https://i.imgur.com/NjPVMnS.png)

### [Coding Plan 排行 - 国内 AI 编程套餐横评](https://coding.mcppla.net/)

一个专注于「AI 编程场景」的中文模型性价比排行榜，由 MCP 社区维护，覆盖国内主流 AI 编程套餐的实际表现对比。榜单从 `代码生成质量`、`补全准确率`、`响应速度` 和 `月费成本` 四个维度综合评分，涵盖 DeepSeek、通义千问、豆包、文心、Kimi 等国产模型以及 GPT、Claude 等海外模型的国内可用方案。每个模型提供 `典型任务样例`（如算法题、前端组件、后端 CRUD）展示实际输出质量，还有 `性价比雷达图` 直观对比各模型在不同场景的优劣。榜单定期更新，用户可按预算范围和用途场景筛选，适合国内开发者在选择 AI 编程助手时做决策参考。

![](https://i.imgur.com/QztejAD.png)

### [UI/UX Pro Max - Claude Code 设计智能数据库与推理引擎](https://github.com/nextlevelbuilder/ui-ux-pro-max-skill)

一款由 NextLevelBuilder 组织开发的 Claude Code 设计智能技能，GitHub 星标超 9 万，是目前 AI 编码生态中最受欢迎的 UI/UX 技能之一。它不是简单的提示模板，而是一个完整的 `可搜索设计数据库 + AI 推理引擎`——内置 `67 种 UI 风格`（玻璃态、粘土态、极简、野蛮主义等）、`161 套行业配色方案`（SaaS、电商、金融、健康等按产品类型匹配）、`57 组 Google Fonts 字体搭配`、`99 条 UX 准则`（无障碍、动效、反模式等）和 `29 种落地页模式`。旗舰功能是 `智能设计系统生成器`：只需输入项目描述，AI 会自动推理出完整设计系统（风格 + 配色 + 字体 + 布局 + 特效），附带 `反模式清单` 和 `交付检查清单`（响应式断点、对比度、SVG 图标等）。支持 React、Next.js、Vue、Flutter、SwiftUI 等 8+ 技术栈，兼容 Claude Code、Cursor、Windsurf、GitHub Copilot 等工具，`npx -y skills add nextlevelbuilder/ui-ux-pro-max-skill` 一键安装，适合需要让 AI 输出专业级界面而非「程序员审美」的前端开发者。

![](https://i.imgur.com/73S1X5w.png)

### [Chandra OCR - 复杂文档结构化识别模型](https://github.com/datalab-to/chandra)

由 Datalab（创始人 Vik Paruchuri）开发的 4B 参数 OCR 模型（当前版本 Chandra 2.1），专为复杂文档设计，在 olmOCR 基准测试中表格识别得分 89.9%，远超同类开源模型。与通用 OCR 不同，Chandra 重点解决 `复杂表格`（合并单元格、嵌套表格、金融报表）、`表单与手写`（复选框、手写笔记、填写表单）、`数学公式` 和 `多栏排版` 等传统 OCR 容易翻车的场景。支持 `90+ 语言`，输出 Markdown、HTML 和 JSON 三种格式并保留完整布局元数据，还能自动提取图表并生成 Mermaid 格式描述。提供 `vLLM`（推荐，GPU 高并发）和 `Hugging Face`（本地 CPU/GPU）两种推理后端，`pip install chandra-ocr` 一行安装即可使用，配套 Streamlit Web App 交互演示。代码 Apache 2.0 开源，Hugging Face CEO 曾用它批量解析 3 万篇 arXiv 论文，适合学术论文提取、合同结构化、发票处理和历史文档数字化等场景。

![](https://i.imgur.com/v4kYsBo.jpeg)

### [Codex2API - Codex 订阅账号池智能 API 网关](https://github.com/james-6-23/codex2api)

一款由开发者 james-6-23 打造的开源 Codex 订阅 API 网关，基于 `Go + Gin` 后端 + `React/Vite` 嵌入式前端构建，GitHub 星标超 1.5k，被社区视为 Sub2API 的轻量替代方案。它将多个 Codex 订阅账号（Refresh Token）聚合为统一的 OpenAI/Anthropic 兼容 API 端点，核心亮点是 `智能调度器`——按账号健康评分、动态并发、冷却恢复和近期用量综合分配请求，429 限流自动冷却、401 封禁自动恢复。暴露 `/v1/chat/completions`、`/v1/responses`、`/v1/messages`、`/v1/images/generations` 等标准接口，支持 `Claude Code`、`Cursor`、`Codex CLI` 等工具直接接入。内置完整 `可视化管理后台`（账号导入/测试、API Key 管理、调度看板、用量统计、生图工作台、请求日志），支持 `per-request upstream isolation` 避免上下文泄漏。提供 PostgreSQL + Redis（生产）和 SQLite + 内存缓存（轻量）两种部署模式，Docker Compose 一行启动，Go 语言实现内存占用远低于同类方案，适合持有多 Codex 订阅且需要在低配服务器上稳定运行的开发者。

![](https://i.imgur.com/cbvc1qg.png)

### [DuckDuckGo MCP Server - DuckDuckGo 搜索与网页抓取 MCP 服务](https://github.com/nickclyde/duckduckgo-mcp-server)

由开发者 nickclyde 打造的开源 MCP 服务器，通过 DuckDuckGo 为 Claude Code、Claude Desktop、Cursor 等 AI 代理提供 `网页搜索` 和 `内容抓取` 双重能力，GitHub 星标超 1.3k。它暴露两个 MCP 工具——`search` 执行 DuckDuckGo 搜索并返回经清洗的 LLM 友好格式结果（标题、URL、摘要，自动去除广告和跳转链接），`fetch_content` 智能抓取并解析网页内容为纯文本。内置 `速率限制`（搜索 30 次/分钟、抓取 20 次/分钟）防止过载，支持 `SafeSearch 过滤`（STRICT/MODERATE/OFF）和 `地区限定`（通过 DDG_REGION 环境变量设置 cn-zh、us-en 等）。内容抓取支持多后端切换——默认 httpx 轻量快速，可选 `curl_cffi 浏览器 TLS 指纹模拟` 绕过 Cloudflare 等 bot 检测。支持 stdio、SSE 和 Streamable HTTP 三种传输协议，`uvx duckduckgo-mcp-server` 一行命令启动，Claude Code 中可用 `claude mcp add ddg-search` 直接集成，MIT 协议开源，适合需要为 AI 代理快速添加隐私友好网页搜索能力的开发者。

1、先安装
```bash
uv pip install duckduckgo-mcp-server
```

2、添加MCP
```json
{
    "mcpServers": {
        "ddg-search": {
            "command": "uvx",
            "args": ["duckduckgo-mcp-server"]
        }
    }
}
```

### [Cockpit Tools - AI IDE 多平台账号与实例管理器](https://github.com/jlcodes99/cockpit-tools)

由开发者 jlcodes99 打造的 Rust + Tauri 桌面工具，专为管理多个 AI 编程 IDE 的账号与实例设计，GitHub 星标超 1.1 万。它覆盖 `12 个主流平台`——Antigravity IDE、Codex、GitHub Copilot、Windsurf、Kiro、Cursor、Gemini CLI、CodeBuddy、Qoder、Trae、Zed 及 Claude 系列产品，通过可视化仪表盘统一展示所有账号状态。核心功能包括 `一键切换账号`（支持 OAuth、Token/JSON、本地导入多种方式）、`实时配额监控`（显示剩余额度、重置时间、用量明细如 Cursor 的 Auto/Composer/API/On-Demand 分类）、`多开实例并行`（独立用户目录 + 启动参数隔离，同一 IDE 同时跑多个实例处理不同项目）和 `自动唤醒任务`（定时触发配额重置）。内置 `设备指纹管理` 降低风控风险，所有数据纯本地存储无需云端，支持批量标签和操作，macOS/Windows/Linux 三平台可用，适合同时重度使用多个 AI 编程工具的开发者。

![](https://i.imgur.com/MQygQkj.png)

### [Jina Reader - 网页转 Markdown 服务](https://jina.ai/reader)

Jina AI 提供的免费网页内容提取服务，只需在任意网页 URL 前加上 `r.jina.ai/` 前缀，即可获得该页面的 `干净 Markdown 内容`，支持 `HTTPS` 和 `HTTP` 协议。服务内置智能内容提取引擎，能自动剥离导航栏、广告、页脚等无关元素，保留正文结构（标题、段落、列表、表格、代码块、图片链接等），输出格式规范的 Markdown，特别适合 `AI Agent 消费`——省去了处理 HTML 噪音的麻烦。支持 `搜索引擎结果页` 提取（`r.jina.ai/https://google.com/search?q=...`）、`GitHub README` 一键获取和 `图片描述生成`，提供 API 接口和 MCP Server 集成，无需注册、免费额度充足，适合需要在工作流中将网页转为结构化文本的开发者和 AI 应用。

![](https://i.imgur.com/df7Zh6E.png)

### [豆包历史对话管理助手 - Chrome 对话管理扩展](https://chromewebstore.google.com/detail/%E8%B1%86%E5%8C%85%E5%8E%86%E5%8F%B2%E5%AF%B9%E8%AF%9D%E7%AE%A1%E7%90%86%E5%8A%A9%E6%89%8B/nmohopddhclnelgfoandhclnmciokcfo)

一款专为字节跳动豆包（Doubao）网页版设计的 Chrome 浏览器扩展，解决豆包对话历史管理的痛点。扩展提供 `对话批量管理`（一键全选、批量删除、按关键词搜索过滤历史对话）、`对话导出`（将对话内容导出为 Markdown、JSON 或纯文本格式方便备份和整理）和 `对话分类标签`（为重要对话添加自定义标签便于后续查找）。界面简洁无侵入，侧边栏形式展示，支持 `暗色模式`，不收集任何用户数据完全本地运行。适合重度使用豆包进行工作和学习、需要经常回顾和管理历史对话内容的用户。

![](https://i.imgur.com/XG4ISbW.png)

### [AIChatIndex - AI 对话目录索引扩展](https://chromewebstore.google.com/detail/aichatindex-%E4%B8%BAchatgpt%E3%80%81%E8%B1%86%E5%8C%85%E3%80%81k/kgaaloljjidblambloblebocaobjjmlb)

一款支持 ChatGPT、豆包、百度文心一言和通义千问等多个 AI 平台的 Chrome 对话索引扩展，核心功能是 `为所有 AI 对话生成可搜索的目录索引`。扩展自动捕获并索引各平台的对话记录，提供 `全文搜索`（支持关键词、正则表达式和语义搜索）、`对话概览面板`（按平台/时间/主题分类展示）和 `快速跳转`（点击索引直接打开对应对话页面）。索引数据完全存储在浏览器本地，支持 `自动清理过期记录` 和 `手动导出索引`。安装后无需配置即可自动开始索引，适合同时在多个 AI 平台工作、需要跨平台检索历史对话内容的用户。

![](https://i.imgur.com/SBPwchy.png)



### [xsai - 极致轻量 OpenAI 兼容 AI SDK](https://github.com/moeru-ai/xsai)

由 moeru-ai 团队打造的 TypeScript AI SDK，主打 `极致轻量`——安装包仅约 142KB，核心模块 `@xsai/generate-text` 更是只有 22.6KB，相比 Vercel AI SDK 安装体积缩小 `40 倍`、打包体积缩小 `13 倍`。接口与 Vercel AI SDK 高度兼容，迁移几乎零成本，但不做全功能框架、不引入通用 provider 抽象，只聚焦 `OpenAI 兼容 API` 的核心能力：文本生成、流式输出、结构化数据生成、工具调用（Function Calling）、嵌入、语音转录与合成、图像生成等。不依赖 Node.js 原生模块，仅基于 Fetch API，浏览器、Edge Runtime、Deno、Bun、Node.js 全平台可运行，适合对体积敏感的前端 AI 应用、边缘 Agent 和 CLI 工具。

```typescript
import { env } from 'node:process'

import { generateText } from 'xsai'

const { text } = await generateText({
  apiKey: env.OPENAI_API_KEY!,
  baseURL: 'https://api.openai.com/v1/',
  messages: [
    {
      content: 'You are a helpful assistant.',
      role: 'system',
    },
    {
      content: 'This is a test, so please answer \'YES\' and nothing else.',
      role: 'user',
    },
  ],
  model: 'gpt-4o',
})

// "YES"
console.log(text)
```

### [Moe Copy AI - AI 网页数据提取助手](https://github.com/yusixian/moe-copy-ai)

一款开源浏览器扩展，由开发者 yusixian 打造，专为将网页内容转化为 AI 模型可直接消费的干净输入而设计。支持 `三种抓取模式`——CSS 选择器模式、Readability 模式和自动优选的混合模式，能智能识别文章主体、标题、作者、元数据和图片等结构化信息，自动清除广告和干扰元素。输出提供原始版与紧凑版两种 Markdown 格式，并显示字数和 Token 统计，方便评估 AI 输入成本。v0.2.0 起新增 `侧边栏批量抓取` 模式，支持一次抓取多个链接、可视化选择器、分页收集和 ZIP 打包导出。内置 xsAI SDK 集成，可直接对提取内容进行 AI 摘要和对话，支持 Chrome/Firefox/手机 Kiwi Browser，中英双语国际化。

![](https://i.imgur.com/LYzCzd6.jpeg)

### [FluxDO - Linux.do 第三方社区客户端](https://github.com/Lingyan000/fluxdo)

由开发者 Lingyan000 基于 Flutter 打造的 Linux.do（L 站）非官方跨平台客户端，覆盖 Android、iOS、macOS、Windows、Linux 五大平台。采用 Material Design 3 设计语言，支持 `动态取色` 和深色模式自动切换，内置 Rust 实现的 `DOH（DNS over HTTPS）代理` 加密 DNS 查询防止污染劫持。完整支持浏览、发帖、回复、搜索、通知、投票等论坛功能，还配备 Markdown 富文本编辑器、图片上传与缓存、代码高亮和 HTML 智能分块渲染。社区特色功能包括 Boots `弹幕化`、待看帖悬浮窗、LDC 积分卡片和内置浏览器，是 L 站用户公认的手机端刷帖体验最佳选择，社区评价「相见恨晚」「丝滑好用」。

![](https://i.imgur.com/kTtJzsk.jpeg)

### [Codex-JetBrains - JetBrains 中运行 VSCode 编码代理](https://github.com/Haleclipse/Codex-JetBrains)

由 WeCode-AI 团队开发的开源 JetBrains 插件，核心解决一个痛点：`在 JetBrains IDE 里直接使用 VSCode 原生的 AI 编码代理`。支持 Cline、Roo Code、Kilo Code 等主流 VSCode-based 代理，覆盖 IntelliJ IDEA、WebStorm、PyCharm、PhpStorm、CLion、GoLand、Rider、Android Studio 等全部 JetBrains IDE（2023.1+）。架构上采用 Kotlin 插件 + Node.js 扩展宿主 + `RPC 通信` 桥接 VSCode API 层与 JetBrains 编辑器，代理在独立 Node.js 进程中运行，与 IDE 无缝集成。安装方式简单，可直接从 JetBrains Marketplace 搜索 `RunVSAgent` 一键安装，适合习惯了 VSCode 代理但想留在 JetBrains IDE 生态中的开发者。

![](https://i.imgur.com/dQeTRPl.png)

### [AI Commits - AI 自动生成 Git 提交消息](https://github.com/Nutlope/aicommits)

由 Nutlope 开发的开源 CLI 工具，GitHub 星标超 9000，运行后自动读取 `git diff` 暂存变更并通过 AI 模型生成专业的 commit 消息，彻底省去手动编写 commit message 的麻烦。支持 `多种 commit 格式`——plain、Conventional Commits、Conventional+body、Gitmoji 和 subject+body，可通过 `--generate N` 一次生成多条备选供挑选。v2 版本新增智能 chunking 处理大型 diff，支持 TogetherAI、OpenAI、Groq、xAI、`Ollama 本地模型`、LM Studio 等多种 AI 提供商，还可通过 `--prompt` 自定义生成风格。提供 `git hook 模式`（`aicommits hook install`），安装后每次 `git commit` 自动生成消息并在编辑器中预览，支持 npm/pnpm/yarn/bun 安装和 Homebrew/AUR 包管理。

快速安装：
```shell
npm install -g aicommits
aicommits setup
```

![](https://i.imgur.com/6isd4w9.png)

### [Aether - GitHub Copilot Chat 多模型增强](https://github.com/OEvortex/better-copilot-chat)

由 OEvortex 开发的 VS Code 扩展（原名 better-copilot-chat），为 GitHub Copilot Chat 接入 `20+ 个 AI 提供商`，包括智谱 GLM、MiniMax、Kimi、DeepSeek、Codex、Mistral、Ollama 等，以及自定义 OpenAI/Anthropic 兼容端点。每个提供商可添加 `无限账号` 并启用负载均衡，速率限制或配额耗尽时自动切换账号、指数退避重试，实现无缝 failover。支持 400k+ 上下文、图片输入、推理模式和 `Web Search 工具调用`（如 `#zhipuWebSearch`）。v0.4.3 起新增 `MCP Tool Bridge`，将 VS Code Language Model tools 暴露给 Claude Code、Cursor 等外部客户端。还提供 FIM 和 NES 高级代码补全模式，快捷键 Ctrl+Shift+Q 快速切换账号，适合需要灵活切换多种模型的 Copilot Chat 用户。

![](https://i.imgur.com/YCoE5SY.png)

### [Unify Chat Provider - VS Code 多 LLM 一键接入 Copilot Chat](https://github.com/smallmain/vscode-unify-chat-provider)

由 SmallMain 开发的 VS Code 扩展，利用 VS Code 官方 `Language Model Chat Provider API`，将 `45+ 个 LLM 供应商`（200+ 主流模型）一键接入 GitHub Copilot Chat 界面。支持 OpenAI、Anthropic、Gemini、DeepSeek、Kimi、GLM、Qwen、Ollama 等全部主流 API 格式，内置每个模型的推荐参数，无需手动调参。特色在于 `免费模型聚合`——可一键添加 GitHub Copilot、Gemini CLI/Antigravity、Claude Code、xAI Grok 等账号配额，实现零成本使用 Claude、Gemini、Kimi 等模型。支持配置导入导出（Base64/JSON/URL）、一键迁移其他应用配置、无限供应商与模型配置共存，可视化配置界面完全嵌入 VS Code 命令面板，兼容工具调用和流式响应等原生特性。

![](https://i.imgur.com/Z5kLIaL.png)

### [Defuddle - 网页正文提取为干净 Markdown](https://github.com/kepano/defuddle)

由 Obsidian 创始人 kepano 开发的开源网页正文提取工具，最初为 Obsidian Web Clipper 打造，后独立为通用库。从任意 URL 或 HTML 中自动识别并保留文章正文，清除广告、评论、侧边栏、导航等杂乱元素，输出干净的 `Markdown`（含 YAML frontmatter 元数据：标题、作者、发布日期、字数统计等）。相比 Mozilla Readability 更宽松、输出更一致，支持移动端样式辅助判断无关元素、HTML 标准化（脚注、MathML、代码块、Obsidian callout）、自定义选择器和调试模式。提供多种使用方式：在线服务（URL 前加 `defuddle.md/` 即可）、CLI（`npx defuddle parse <url> --markdown`）、浏览器 Bookmarklet 和 Node.js API，适合网页剪藏、AI Agent 预处理和知识库构建。

```javascript
import Defuddle from 'defuddle';

// Parse the current document
const defuddle = new Defuddle(document);
const result = defuddle.parse();

// Access the content and metadata
console.log(result.content);
console.log(result.title);
console.log(result.author);
```

### [XCloud影视 - 在线影视解析播放](https://www.xcloudv.top/)

个人自建的在线影视聚合解析平台，基于开源 vip-video-player 项目，覆盖爱奇艺、腾讯视频、优酷、芒果TV 等主流平台。支持 `全平台搜索`（独立搜索站 so.xshyun.top）和多个播放线路切换（量子、虎牙、茅台等资源源），无需注册即可观看。特色功能包括 `追剧提醒`（检测更新后通知）、播放历史记录与进度记忆、自动下一集、随机电影推荐和深色模式切换。PC 与手机端均适配，支持投屏和视频分享。站点声明不存储任何视频资源，内容仅供学习参考。

![](https://i.imgur.com/KeVB0Nx.png)

### [WeChatTweak - macOS 微信撤回拦截与多开](https://github.com/tanranv5/WeChatTweak)

由 tanranv5 维护的开源命令行工具（sunnyyoung/WeChatTweak 的活跃 Fork），专为 macOS 微信客户端提供两大核心功能：`撤回拦截`（好友/群聊撤回的消息仍可见，可自定义高亮显示）和 `无限多开`（Dock 栏右键微信图标即可登录新账号，无需手机二次认证）。通过动态库注入方式实现，支持 `阻止自动更新`、消息列表通知和 Alfred/Raycast 集成。可通过 Homebrew 一键安装（`brew install tanranv5/tap/wechattweak`），执行 `wechattweak patch` 后重启微信即生效。适配 Apple Silicon 和 x64 双架构，持续跟进微信最新版本，适合需要在 macOS 上同时管理多个微信账号或防撤回的用户。



### [ncm-api-rs - 网易云音乐 Rust 原生 API 服务](https://github.com/imsyy/ncm-api-rs)

由 imsyy 开发的纯 Rust 实现网易云音乐 API 后端服务，从 Node.js 版 NeteaseCloudMusicApi Enhanced 完整移植，提供 `300+ 个接口`，覆盖搜索、播放、歌单、用户、动态等全部功能。核心优势是极致轻量——内存占用仅约 `5MB`（Node.js 版通常 50-100MB），基于 tokio + reqwest 实现 `异步非阻塞` 请求，支持 weapi/eapi/linuxapi 三种加密方式。提供 `HTTP 服务模式`（内置 Axum 服务器，接口路径与 Node.js 版完全一致，前端无缝切换）和 Rust 库模式两种用法，还支持 HTTP/SOCKS5 代理和 `IP 伪装`（randomCNIP），解决国外服务器 460 限制。适合自建音乐 API 后端、音乐播放器集成或资源受限的部署环境。

```rust
use ncm_api_rs::{create_client, Query};

#[tokio::main]
async fn main() {
    // 创建客户端（不带 cookie）
    let client = create_client(None);

    // 搜索歌曲
    let query = Query::new()
        .param("keywords", "晴天 周杰伦")
        .param("type", "1")       // 1=歌曲
        .param("limit", "10");
    let result = client.cloudsearch(&query).await.unwrap();
    println!("{}", result.body);

    // 获取歌曲详情
    let query = Query::new().param("ids", "186016");
    let detail = client.song_detail(&query).await.unwrap();
    println!("{}", detail.body);

    // 获取歌词
    let query = Query::new().param("id", "186016");
    let lyric = client.lyric(&query).await.unwrap();
    println!("{}", lyric.body["lrc"]["lyric"]);

    // 获取播放链接
    let query = Query::new()
        .param("id", "186016")
        .param("level", "standard");
    let url = client.song_url_v1(&query).await.unwrap();
    println!("{}", url.body);
}
```

### [API Clawer - 网易云音乐客户端抓包工具](https://github.com/NeteaseCloudMusicApiEnhanced/api-clawer)

NeteaseCloudMusicApiEnhanced 组织官方提供的简易抓包工具，专为帮助开发者贡献网易云音乐接口而设计。通过在本地运行抓包代理服务器（默认端口 9000）和前端显示服务器（默认端口 3000），将网易云音乐 PC 客户端的流量代理到本地，`实时捕获并解密` 客户端的所有 HTTP 请求和响应数据（支持 WeAPI/EAPI 等加密方式），以 JSON 格式输出。浏览器访问 `localhost:3000` 即可查看实时抓包记录，点击可查看完整的 URL、请求头、请求体、响应状态码和响应体。适合逆向分析网易云音乐接口参数变化、完善开源 API 文档或调试加密逻辑的开发者。



### [Process Explorer - 微软 Sysinternals 进程管理器](https://learn.microsoft.com/en-us/sysinternals/downloads/process-explorer)

微软官方 Sysinternals 工具套件中的免费进程管理利器，被誉为 Windows 任务管理器的 `超强替代品`，由 Mark Russinovich 开发后被微软收购维护。提供 `树形进程视图`（父子关系一目了然）、句柄和 DLL 模式（查看进程打开的所有文件/注册表键/网络连接及加载的模块）、实时 CPU/GPU 监控、线程堆栈跟踪和内存转储生成等深度诊断能力。底部面板可快速搜索定位哪个进程持有了被锁定的文件，支持一键 `终止整个进程树`、挂起/恢复进程、VirusTotal 在线验证。免安装，直接运行即可使用，适合排查文件占用、内存泄漏、恶意软件分析和系统性能调优，是 Windows 开发者和系统管理员的必备工具。

![](https://i.imgur.com/DGmLBZ5.png)

### [幻觉翻译 - arXiv 论文在线翻译工具](https://hjfy.top/)

一款专注于 arXiv 学术论文翻译的在线工具，核心优势在于基于 `LaTeX 源码逐段翻译`，完美保留原论文的排版、公式、图表和引用，输出格式与原文完全一致（非 PDF OCR 翻译）。输入论文 ID 或链接即可获取中文翻译结果，支持输出中文 LaTeX 源码便于二次编辑。开发者已预翻译上万篇 Hugging Face 热门论文（消耗约 3 亿 token），覆盖度极高。还支持 PDF、Word、Epub 等普通文档翻译（OCR 方式）。提供 Zotero 插件实现一键翻译，每日限额 arXiv 100 篇、PDF 10 篇，免费使用需登录。X 平台和中文技术社区广泛推荐为「目前最好用的 arXiv 论文翻译网站」。

![](https://i.imgur.com/6dPSRYg.png)

### [yt-dlp GUI - 现代跨平台视频下载器](https://github.com/imsyy/yt-dlp-gui)

由 imsyy 开发的开源桌面视频下载器，基于 `Tauri 2 + Vue 3` 构建，安装包仅约 10MB，内存占用极低，支持 Windows/macOS/Linux 三平台。封装 yt-dlp 的全部能力，覆盖 YouTube、Bilibili、TikTok、Twitter/X 等 `1000+ 站点`。粘贴链接即可自动预览标题、封面、时长和可用格式，支持下载队列管理、播放列表批量下载、实时进度显示。内置 `工具箱` 包括缩略图下载器、字幕提取器（SRT/VTT/ASS/LRC，支持双语合并）、YouTube 直播弹幕归档和插件管理器。高级功能涵盖时间剪辑下载、格式转码（MP4/MKV/MP3）、`SponsorBlock 自动跳过广告`、Cookie 认证、代理和下载限速，配套浏览器扩展实现一键发送链接。

![](https://i.imgur.com/AgEYPQ4.png)

### [LeeSin - 英雄联盟跨平台桌面助手](https://github.com/lzskyline/LeeSin)

基于 `Electron + React` 重构自开源项目 Seraphine 的英雄联盟辅助工具，支持 Windows/macOS/Linux 三平台。完全基于 Riot Games 官方 `LCU API` 实现，无需修改游戏文件或内存读写。核心功能包括：自动接受对局（可自定义延迟）、自动 BP 禁选英雄、从 `OP.GG 实时获取推荐符文和出装` 并一键应用。支持排位、匹配、极地大乱斗、`符文大乱斗（ARAM Mayhem）`、斗魂竞技场、无限火力等全部模式。内置召唤师战绩查询、队友/对手自动查询、英雄排行榜、客户端热重启和秒退等辅助功能。技术栈采用 React 18 + TypeScript + TailwindCSS + Vite，相比原版 Seraphine 启动更快、内存占用更低，UI 全面现代化。

![](https://i.imgur.com/P2DyD6F.png)

### [1Music - 免费高品质音乐下载](https://1music.cc)

一个极简的免费音乐下载网站，支持 `FLAC 无损` 和 MP3 高品质音源，无需注册即可搜索下载。曲库覆盖面广，搜索精准，附带完整的专辑封面和歌曲元信息。界面简洁干净、无广告干扰，提供中文界面（/zh-CN）。特色功能是 `一键上传至 WebDAV`，方便将下载的音乐直接存储到 NAS 或个人服务器，可与 Navidrome、音流等自建音乐播放器无缝配合。在各大平台版权收紧、会员费用高企的当下，被 X 平台和中文技术社区广泛推荐为「宝藏站」。资源类站点稳定性存疑，建议有需要时及时收藏。

![](https://i.imgur.com/L066NSO.png)

## \>\> 🐱跟着大橘随便看看

### 分享2个最近觉得比较好玩的GPT Image2 Prompt

1、转换风格为涂鸦

```
根据参考图创作丑萌沙雕涂鸦插画,平价蜡笔马克笔随手潦草 手绘,线条杂乱随性,比例透视扭曲失衡,五 官夸张滑稽,涂色溢出、上色不均、明显笔触, 画面点缀杂乱无意义碎线条,简约漫画背景, 敷衍随性画风,表情包趣味风格,拒绝写实精 致规整,图片比例保持不变。
```

效果如下：

![](https://i.imgur.com/pyYGLpY.jpeg)

2、也是另外一种涂鸦转换：

```
Please transform the entire image into a single Decorative Folk Flat Illustration with Doodle elements. Use a bold and playful color palette, completely different from the original image. Simplify all details into clean, flat shapes with a handmade, slightly imperfect feel, as if drawn on a sheet of white paper. The overall style should look cute, childlike, and whimsical
```

效果如下：

![](https://i.imgur.com/L4d6SB4.png)

### [Grokipedia - xAI 推出的 AI 驱动百科全书](https://grokipedia.com)

由 Elon Musk 的 AI 公司 xAI 运营的在线百科全书，2025 年 10 月上线，目标是成为 Wikipedia 的替代品。文章主要由 `Grok 大语言模型生成和事实核查`，用户不可直接编辑但可提交修改建议由 AI 审核。上线仅数月条目已从约 80 万飙升至 `600 万+`，覆盖面极广。采用 xAI 自有的 X Community License（非商业可自由使用），部分改编自 Wikipedia 的条目保留 CC-BY-SA 4.0 许可。Musk 计划未来将其改名为 Encyclopedia Galactica，甚至把副本发送到月球和火星。不过该项目争议不小——多家媒体指出其存在事实错误、引用低可信来源（如 Instagram 短视频、极端论坛）以及内容偏向右翼观点的问题，准确性和编辑透明度仍是核心挑战。



### [Anthropic 发布防蒸馏攻击新研究成果](https://www.anthropic.com/research/detecting-and-preventing-distillation-attacks)

Anthropic 发布的一篇关于检测和防止模型蒸馏攻击的技术研究。蒸馏攻击指攻击者通过大量调用目标模型的 API 来「蒸馏」出近似能力的廉价替代模型，本质上是一种新型知识产权窃取。这篇论文提出了一套多层次的防御框架：从 `请求模式指纹识别`（检测异常调用频率和 prompt 分布）到 `水印注入`（在模型输出中嵌入不可见标记追踪泄露源头），再到 `输出扰动`（对高频调用者动态微调响应分布使蒸馏出的模型质量下降）。研究还讨论了防御与 `模型可用性` 之间的权衡——太激进的保护会伤害正常用户，文章给出了在真实部署环境下的实验数据和推荐配置，对运营自有模型 API 的公司有直接参考价值。



### [V2EX 讨论：如何让 B 站评论区更好用](https://www.v2ex.com/t/1100239)

V2EX 上的一则热帖，讨论 B 站评论区体验的痛点以及社区开发者们做出的改善方案。帖子里汇集了多款 `浏览器扩展` 和 `用户脚本`，主要解决几个高频问题：`按热度/时间/回复数排序评论`、`过滤关键词和低质评论`、`折叠已看过的长评论链`、`在评论区直接预览图片和视频链接`、`显示评论者等级和标记` 等。讨论中推荐了几款成熟插件（如 Bilibili Evolved 的评论增强模块和独立的 CommentEnhancer），还涉及 B 站 API 限制带来的技术挑战，对经常泡在 B 站评论区获取技术内容或参与讨论的用户有实用参考价值。



### [Sci-Hub 推出 AI 学术问答助手 Sci-Bot](https://sci-bot.ru/)

Sci-Hub 在其超过 8500 万篇全文论文数据库基础上推出的 AI 学术研究助手，核心能力是 `基于真实论文全文生成答案`——用户用自然语言提问（支持多语言），Sci-Bot 检索相关文献、合成结构化回答并附带 `可点击的引用链接` 直接跳转原文 PDF，相比通用 LLM 大幅减少幻觉风险。支持文献综述、方法比较、机制解释等复杂查询，有对话历史和 Token 消耗管理系统。该项目在学术界引发广泛讨论——支持者视其为 `知识民主化` 的延续（尤其帮助发展中国家研究者），批评者则关注其版权争议，C&EN 等权威期刊已做专题报道。网站为 sci-bot.ru，免费使用有配额限制，可通过捐赠获取更多额度。

### [面向对象设计原则与模式 - 中文开源教程](https://github.com/knightsj/object-oriented-design)

一份系统完整的中文面向对象设计教程仓库，覆盖六大经典设计原则（`开闭原则`、`单一职责`、`依赖倒置`、`接口分离`、`迪米特法则`、`里氏替换`）和全部 23 种 `GoF 设计模式`（创建型、结构型、行为型三类齐全）。每个原则和模式都配备中文定义解读、`UML 类图对比`（改造前 vs 改造后）和可直接运行的 `Java 代码 Demo`，结构清晰适合逐章学习。配套掘金博客系列文章含更详细的图文解释，项目完全开源免费，适合 Java 开发者系统学习 OOD 基础、面试备考或团队内部分享教学。