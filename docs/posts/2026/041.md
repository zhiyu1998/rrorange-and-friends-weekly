---
title: 第041期 - 晴云化狐
date: 2026-07-12
cover: https://i.imgur.com/dJtZIEV.jpeg
created: 2026-07-12 00:00:00
updated: 2026-07-12 13:53:00
---

# 第041期 - 晴云化狐

![](https://i.imgur.com/dJtZIEV.jpeg)

这个是这周下午下班的时候拍到的晚霞，很像一只狼 or 狐，所以我让GPT给我重绘了一下这个图片然后就有了这期封面

---

绘制过程：

![](https://i.imgur.com/oH2wQpq.png)

## \>\> 🐈大橘的工具集

### [x.ai CLI - 免费的 x.ai 命令行接口，支持 API 调用与自动化脚本](https://x.ai/cli)
`x.ai CLI` 是 x.ai 官方推出的免费高级命令行接口，拥有完整的 `xAI API` 权限，支持文本生成、检索和文档创建等操作，无需付费即可获得持续的免费额度。支持中文等多语言，提供 `--token`、`--run` 等丰富参数选项，适合开发者编写自动化脚本进行批量数据处理、消息处理和复杂计算任务，是调用 `Grok` 模型能力的轻量级命令行入口。

![](https://i.imgur.com/ra6Eroc.png)

### [mise - 一站式开发工具管理与任务运行器](https://github.com/jdx/mise)
`mise`（原名 rtx）是一款集**开发工具版本管理**、**环境变量加载**和**任务运行器**于一体的项目级配置工具。通过单个 `mise.toml` 文件即可管理 Node.js、Python、Rust 等 1000+ 工具的版本切换，自动处理 PATH 配置，还支持项目级环境变量（类似 direnv）和带依赖图的任务编排，是替代 `pyenv + direnv + make` 的统一方案。
![](https://github.com/jdx/mise/raw/main/docs/tapes/demo.gif)

### [N_m3u8DL-RE - 功能强大的跨平台流媒体下载工具](https://github.com/nilaoda/N_m3u8DL-RE)

一款跨平台、功能全面的流媒体下载工具，支持 `MPD`（DASH）、`M3U8`（HLS）和 `ISM`（MSS）三种主流流媒体格式，涵盖点播下载与直播录制场景。基于 `.NET` 开发并编译为原生二进制，体积小巧无需额外依赖，具备多轨并发下载、自动轨道选择、`AES-128` / `CENC` 加密解密、实时混流等高级功能，支持 Windows、Linux、macOS 及 Android Termux 等多个平台。

```
Description:
  N_m3u8DL-RE (Beta version) 20251027

Usage:
  N_m3u8DL-RE <input> [options]

Arguments:
  <input>  链接或文件

Options:
  --tmp-dir <tmp-dir>                                     设置临时文件存储目录
  --save-dir <save-dir>                                   设置输出目录
  --save-name <save-name>                                 设置保存文件名
  --save-pattern <save-pattern>                           设置保存文件命名模板, 支持使用变量: 
                                                          <SaveName>, <Id>, <Codecs>, <Language>, <Resolution>, 
                                                          <Bandwidth>, <MediaType>, <Channels>, <FrameRate>, 
                                                          <VideoRange>, <GroupId>, <Ext>
                                                          示例: --save-pattern "<SaveName>_<Resolution>_<Bandwidth>"
  --log-file-path <log-file-path>                         设置日志文件路径, 例如 C:\Logs\log.txt
```

### [Kaku - 为 AI 编程设计的 macOS 终端](https://kaku.fun/)
`Kaku` 是一款基于 WezTerm 深度定制的 macOS 开源终端，专为 AI 编程场景设计。核心亮点是内置的 `Kaku Assistant`：命令执行失败时自动分析错误并 inline 显示修复建议（`Cmd + Shift + E` 一键应用），支持用 `# <描述>` 自然语言生成 Shell 命令，还提供 `Cmd + L` 打开的 AI Chat 面板用于项目上下文对话。开箱即用集成 Starship、Lazygit、Yazi 等工具，可通过 `kaku ai` 统一管理 Claude Code、Codex、Gemini CLI 等多个 AI 工具。

![](https://i.imgur.com/bvmUER0.png)

### [TinyFish AI - AI Web Agent 的企业基础设施平台](https://www.tinyfish.ai/)
`TinyFish AI` 是一个专为 AI Agent 设计的企业级 Web 基础设施平台，提供统一的 `Web Search API`、`Fetch API`、`Browser API` 和 `Web Agent` 四大核心功能，让 AI 能够安全高效地访问和操作实时网络。平台内置基础设施级反爬虫保护（自动绕过 Cloudflare 等），支持 stealth 模式远程浏览器会话和多步自主代理，通过一个 `API Key` 和一步式信用系统统一管理所有成本，适合报价自动化、竞争分析、数据监控等生产级场景。

![](https://i.imgur.com/yTf5XDz.png)

### [Pi-switch - Pi Agent 模型服务商图形化配置工具](https://github.com/Wing900/Pi-switch)
`Pi-switch` 是一款基于 `Wails` 构建的轻量级、跨平台桌面应用，专为 `Pi Agent`（一个极简 Coding Agent）设计的模型服务商配置工具。支持通过图形化界面管理多个 `LLM Provider`（如 OpenAI、Anthropic、Groq、Ollama、xAI、Kimi 等），一键拉取可用模型列表（含 `contextWindow`、`maxTokens` 等信息），并可快速设置默认模型后一键启动 Pi Agent，免去手动编辑 `models.json` 的繁琐步骤，体积小巧即装即用。

![](https://i.imgur.com/0O7Uem9.png)

### [SkillClaw - 让 AI Agent 的技能在交互中自动演化的开源框架](https://github.com/AMAP-ML/SkillClaw)
`SkillClaw` 是一个开源的 AI 框架，核心理念是让 LLM Agent 使用的 `技能（skills）` 不再是静态不变的，而是通过**多用户真实交互轨迹集体演化**。框架内置的 `Agentic Evolver` 会自动分析成功与失败的会话轨迹，识别重复行为模式，对现有技能进行精炼或自动新增，更新后的技能推送到所有代理中即时生效。用户只需正常聊天，技能库就会在后台静默进化，实现**经验复用与持续学习**，适合需要长期积累和优化 Agent 能力的团队场景。

![](https://i.imgur.com/cPaH5rl.png)

### [Bettbox - 跨平台 Mihomo 内核代理客户端](https://github.com/appshubcc/Bettbox)
`Bettbox` 是一款开源的跨平台 `Mihomo`（Clash Meta）内核代理客户端，基于 FlClash 重构而来，定位"Better Experience"，专注于流畅的前台 UI、后台省电无感运行和长期稳定性。支持 Windows、macOS、Linux、Android、Android TV 及 HarmonyOS NEXT，兼容 Shadowsocks、Trojan、VMess、VLESS、Hysteria、TUIC、WireGuard 等主流协议，提供可视化设置（改动即时生效）、智能启停、一键禁用 QUIC、首页小组件实时网速显示等增强功能，通过 `SignPath` 安全审计，完全开源无广告。

![](https://i.imgur.com/1mfiggi.png)

### [Cache Hit Rates for Agents - LLM 推理代理的缓存命中率排行榜](https://dirac.run/posts/cache-hit-rates-agents)
一篇深入分析 AI `Agent` 场景下 `LLM` 推理成本优化的指南。文章核心观点是：在多轮代理交互中，`缓存命中率`（`cache hit rate`）比选择更便宜的模型更能决定实际推理成本——缓存命中时输入 token 费用通常仅为未缓存的 1/10。文中系统梳理了三种缓存策略：`Prefix/KV Cache`（共享前缀，适合系统提示固定的代理）、`Semantic Cache`（语义相似度匹配）、`Agentic Plan Caching`（从历史计划模板提取复用，可降低 46.62% 成本并保持 96.67% 性能），并基于 2026 年实测数据对比了 DeepSeek（87%）、Anthropic（79-89%）等提供商的缓存表现，最终给出了多层缓存组合的优化方案。

![](https://i.imgur.com/cEV11N5.png)

### [Dinky - macOS 免费开源文件压缩工具](https://dinkyfiles.com/)
`Dinky` 是一款专为 macOS 设计的免费开源文件压缩工具，支持 `JPG`、`PNG`、`WebP`、`AVIF`、`HEIC`、`MP4`、`WAV`、`PDF` 等多种格式的一键压缩。核心特色在于自动识别照片与截图并智能选择最优压缩策略，支持格式自动转换（如 JPG 转 WebP/AVIF）、批量处理、`Watch Folder` 监视文件夹自动压缩、`拖拽/粘贴`（`Cmd+Shift+V` 系统级快捷键）等高效工作流功能。安装包仅 35MB，全程本地运行无需上传云端，可通过 `Homebrew` 一键安装（`brew install --cask dinky`），适合 Mac 用户日常压缩截图、照片、视频等文件的场景，压缩效果可达 50 倍以上。

![](https://i.imgur.com/rpxrkzn.png)

### [pi-toolset - Pi Agent 极简终端 AI 编程助手扩展工具集](https://github.com/balaenis/pi-toolset)
`pi-toolset` 是 Pi 编程助手框架的扩展包集合，为 `Pi Agent` 这个极简终端 AI 编程助手提供丰富的功能扩展。Pi 本身仅内置 `read`、`write`、`edit`、`bash` 四个核心工具，系统提示词不到 1000 token，通过 `Pi Packages` 扩展机制可一键安装社区包——例如 `pi-web-access`（网页搜索与抓取）、`pi-agent-browser-native`（浏览器自动化）、`pi-hermes-memory`（持久化记忆与搜索）等 500+ 第三方扩展，还支持会话树分支、上下文压缩和指令工程（`AGENTS.md` / `SYSTEM.md` / Skills），适合需要按自身工作流定制 AI 编程助手的开发者。

> L站某位佬友分享的一些 PI + Grok + GPT的使用经验所在的GitHub库

![](https://i.imgur.com/qvityS2.png)

### [pi-switch - Pi Agent 多模型服务商切换与智能路由网关](https://github.com/CallmeLins/pi-switch)
`pi-switch` 是一款专为 `Pi Agent` 设计的 `Provider Profile` 管理工具，提供 `TUI` 和 `CLI` 双模式操作。核心亮点是本地 `Model-Name Gateway` 代理网关——通过 `"provider-name/model-id"` 格式动态路由请求，支持自动 `failover`（失败时按链路顺序切换提供商）、`Circuit Breaker` 断路器保护和 `SSE` 流式传输，还内置了 `Claude Code`、`Codex`、`Gemini` 等 User-Agent 伪装预设。交互式 TUI 基于 `ratatui` 渲染，支持 Vim 键位和 Dracula 主题，同时提供诊断检查、请求统计、配置备份加密导出等实用功能，让重度使用多模型的开发者告别手动编辑 `models.json` 的繁琐流程。

![](https://i.imgur.com/R0QMcUQ.png)

### [witr - 为什么这个进程在运行？](https://github.com/pranshuparmar/witr)
`witr`（Why Is This Running?）是一款面向 Linux 和 macOS 的**进程溯源诊断工具**，能将任意进程、服务或端口追踪回其来源和责任链，快速解释这个进程是什么、为什么在运行。核心功能包括实时可排序/过滤的进程列表、端口视图、进程祖先前谱分析等，大幅缩短排查进程的时间，特别适合生产环境故障排查、安全审计和日常系统维护场景。

![](https://i.imgur.com/I3azliW.png)

### [claude-tap - AI 编码代理的 API 流量拦截与跟踪查看器](https://github.com/liaohch3/claude-tap)
`claude-tap` 是一个本地代理加跟踪查看器，专为 AI 编码代理设计，用于拦截和实时检查代理的 API 流量与上下文。支持 `Claude Code`、`Codex`、`Gemini CLI`、`Kimi CLI`、`Cursor` 等多个主流工具，能捕获完整 API 流量（包括系统提示、对话历史、工具调用、流式响应、token 使用等），并生成内置的本地 HTML 跟踪查看器，支持搜索、结构化差异比较和深色模式，全程本地运行无需上传云端，是调试 AI Agent 行为的利器。

![](https://i.imgur.com/Z2Bgetp.png)

### [yacd - 又一个 Clash Dashboard 网页仪表盘](https://github.com/haishanh/yacd)
`yacd`（Yet Another Clash Dashboard）是一款开源免费的 `Clash`/`Mihomo` 代理工具 Web 管理界面，提供节点切换、延迟测试与监控、流量统计、连接实时查看、规则管理与命中统计等核心功能。支持 `dark`/`light`/`auto` 主题切换，可通过 `Docker`、`Nginx` 或静态文件多种方式部署，也提供官方在线实例 `yacd.metacubex.one` 直接使用，是 Clash 生态中最经典且广泛使用的可视化管理面板之一。

![](https://i.imgur.com/kNV27x7.png)

### [Waza - 把工程习惯变成AI编码代理的可执行技能](https://github.com/tw93/Waza)
`Waza` 是由 `tw93` 开发的开源项目，核心理念是将工程师日常的 `工程习惯（engineering habits）` 转化为 `Claude Code`、`Codex` 等 AI 编码代理的可执行 `技能（skills）`。提供 8 个极简技能（`/think`、`/ui`、`/check`、`/hunt`、`/write`、`/learn`、`/read`、`/health`），通过 `npx skills add tw93/Waza` 一键安装，支持链式使用，让 AI 代理像资深工程师一样思考、调试和审查代码。

![](https://i.imgur.com/9LbGz4n.png)

### [zcode-keysmith - 为 ZCode App 持久化注入 system role 的管理工具](https://github.com/Jia-Ethan/zcode-keysmith)
`zcode-keysmith` 是为 `ZCode App`（Z.ai 的桌面端编码代理应用）设计的 **持久化 managed system-role.md 工具**，通过修改 `ZCODE_AGENT_SERVER_COMMAND` 环境变量启动 `zcode-agent-wrapper.py`，将 `~/.zcode-keysmith/system-role.md` 作为 `system message` 注入给 `GLM-5.2` 模型。提供 `install`、`doctor`、`verify`、`uninstall` 等命令，让 ZCode 的 `system role` 可以像 `CLAUDE.md` 一样真正持久化且可维护，无需每次手动复制。

> 下面是快速安装指南，直接发给agent

```
请使用 https://github.com/Jia-Ethan/zcode-keysmith 帮我安装 ZCode App 的 managed true system-role entrypoint。

执行要求：
1. 先阅读 README.md 和 zcode-keysmith.py。
2. 运行：python3 zcode-keysmith.py install --dry-run。
3. 向我展示将写入的准确路径，必须包括：
   - ~/.zcode-keysmith/system-role.md
   - ~/.zcode-keysmith/config.json
   - ~/.zcode-keysmith/bin/zcode-agent-wrapper.py
   - ~/.zcode-keysmith/bin/zcode-keysmith-env.sh
   - ~/Library/LaunchAgents/com.jia.zcode-keysmith.env.plist
4. 同时展示将使用的 ZCode runtime 路径、ZCode node command 路径、agent-server args，以及 app_bundle_modified: false。
5. API key、token、MCP 配置、ZCode provider 配置由 ZCode 自身管理；安装器不读取、不保存、不打印这些内容。
6. 只问我一次：是否确认写入以上持久化入口文件。
7. 我确认后，运行：python3 zcode-keysmith.py install --yes。
8. 写入后运行：python3 zcode-keysmith.py doctor。
9. 再运行：python3 zcode-keysmith.py verify。
10. 提醒我重新打开 ZCode，然后新建任务测试“你是谁”。测试后再次运行 verify，确认 wrapper_invoked: true。
```

### [ian-xiaohei-illustrations - 为中文文章生成怪诞手绘风配图的 Codex Skill](https://github.com/helloianneo/ian-xiaohei-illustrations)
`ian-xiaohei-illustrations` 是一个开源的 `Codex Skill`，专为中文文章、博客、Notion 文档等内容生成 16:9 横版怪诞手绘风正文配图。固定 IP 形象"小黑"（黑色实心小怪物）以参与者而非装饰者的姿态融入画面，采用白底黑色线稿搭配少量红橙蓝中文手写批注，通过大量留白营造清爽怪诞但不幼稚的视觉风格，能把文章中的判断、流程、结构和隐喻变成一张张有记忆点的解释图。安装后只需一行提示词即可调用，支持先输出 `shot list` 规划配图方案再批量生成，交付的 PNG 按编号自动存入 `assets/` 目录，适合中文技术博客和方法论内容的配图需求。

![](https://i.imgur.com/GtWzvdq.png)

### [NyaTerm - 现代化远程终端工作空间客户端](https://github.com/nyakang/nyaterm)
`NyaTerm` 是一款基于 `React + Tauri + Rust` 技术栈构建的现代桌面远程终端工作空间客户端，专为 SSH 中心操作和混合终端工作流设计。支持 SSH、本地 Shell、Telnet、串口等多种会话类型，提供多标签工作空间与可拖拽的水平/垂直分割面板，内置 `SFTP` 文件浏览器（支持远程文件本地编辑与自动上传）、实时主机资源监控、`AI 助手`（Ask 模式生成命令与 Agent 模式多步工作流）、加密云同步（WebDAV/S3/Gist）等丰富功能，同时覆盖密码与密钥认证、跳板机、隧道、X11 转发等安全网络能力，适合需要在一个统一界面中同时管理多台远程服务器的开发和运维人员使用。

![](https://i.imgur.com/A2ZOgzG.png)

### [cscience - 本地运行 Claude Science 的 API Key 补丁版](https://github.com/Haleclipse/cscience)
`cscience` 是一个对 Claude Science 前端打包产物进行 `AST` 级补丁的开源工具，核心目的是将原本仅支持 `OAuth` 认证的 Claude Science 改造为支持自定义 `Anthropic API Key` 本地运行。补丁器基于 `Acorn` 解析约 9MB 的压缩 JS 产物为完整语法树，逐一应用 13 个针对性补丁（包括 OAuth 门控绕过、环境变量回退、模型列表自定义等），并验证输出可正常解析。安装后通过 `cscience serve` 即可启动本地服务并自动打开浏览器，还支持 `OPERON_MODELS` 环境变量自定义可用模型列表、`ANTHROPIC_BASE_URL` 配置代理端点等灵活配置，适合希望在本地完全掌控 Claude Science 使用体验的开发者。

![](https://i.imgur.com/RE4ZgsK.png)

### [YoutubeTweak - 高度可定制的 YouTube 观看体验增强插件](https://github.com/xlch88/YoutubeTweak)
`YoutubeTweak` 是一款开源免费的浏览器扩展，支持 `Firefox`、`Chrome`、`Edge` 等主流浏览器，专为重度 YouTube 用户打造全方位的观看体验增强。核心功能包括播放器层面的画质锁定（自动降级到最接近清晰度）、按频道记忆播放速度、音量增强至真实 100%、隐藏结尾推荐等；评论区方面支持显示频道昵称、自动展开长评论与自动翻译；此外还提供首页视频布局锁定、实验性广告自动跳过等实用特性。所有功能均可按需开关，支持设置导入导出，完全开源无广告无追踪，是优化 YouTube 日常使用效率的利器。

![](https://i.imgur.com/wr05IRr.png)

### [Save-Restricted-Bot - Telegram 受限内容保存机器人](https://github.com/bipinkrish/Save-Restricted-Bot)
`Save-Restricted-Bot` 是一款基于 `Pyrogram` 开发的 `Telegram` 机器人，能够通过帖子链接获取并转发频道或群组中的受限内容（包括文本、媒体和文件）。支持公共频道直接发送链接获取，私有群组则需通过 `Telegram` 账号登录授权后操作，内置批量下载功能，是 Telegram 用户备份和保存受限频道内容的实用工具。

> 自行测试，挺好用的

![](https://i.imgur.com/xgy8jIo.png)

### [Aerion - 轻量级跨平台开源邮件客户端](https://github.com/hkdb/aerion)
`Aerion` 是一款基于 `Wails + Svelte` 技术栈构建的轻量级开源邮件客户端，以极低的 CPU、内存和电池占用为设计理念，定位为 `Geary` 和 `Thunderbird` 的轻量替代品。原生支持 `Linux`（优先）、macOS 和 Windows 三平台，提供会话线程浏览、富文本 `WYSIWYG` 编辑器、多账号统一收件箱（兼容 Gmail、Outlook、`IMAP/SMTP` 等主流协议）、`CardDAV` 联系人同步、`Vim` 风格键盘快捷键导航等核心功能，支持深色模式且无需依赖 `GNOME Online Accounts` 等系统服务，追求简洁高效的邮件收发体验。

![](https://i.imgur.com/O9Y74rw.png)

### [jimmy - 50+ 笔记格式一键转 Markdown 的跨平台转换器](https://github.com/marph91/jimmy)
`jimmy` 是一款开源免费的笔记格式转换工具，支持将 `Notion`、`Evernote`、`OneNote`、`Google Keep`、`Joplin`、`Obsidian` 等 50+ 款笔记应用的导出包以及 `Pandoc` 支持的所有格式（PDF、DOCX、HTML、TXT 等）统一转换为带 `Frontmatter` 元数据的 `Markdown` 文件。转换过程完整保留笔记内容、标签、资源图片、附件和笔记链接，提供 `CLI` 和 `TUI`（交互界面）双模式操作，无需安装 Python 或 Node.js，下载二进制文件即可跨平台离线运行，是笔记迁移和长期归档的实用利器。

![](https://i.imgur.com/uc26y4b.png)

### [CLI-Manager - 集中管理多个 CLI 开发项目的桌面应用](https://github.com/dark-hxx/CLI-Manager)
`CLI-Manager` 是一款专为 `Claude Code`、`Codex CLI` 等 AI 命令行工具设计的跨平台桌面应用（Windows 完整支持，macOS/Linux 实验性），核心解决多窗口终端切换烦琐和重复输入命令的痛点。内置高性能终端支持**自由分屏布局**（左右/上下/嵌套）、Tab 拖拽和自定义背景，集成 AI Hook 实时通知（Claude 权限审批桌面弹窗提醒）、Token 用量统计与费用估算、历史会话 Diff 可视化，还提供 `WebDAV` 云同步、`Git Worktree` 并行隔离、用量分析看板等高级功能，通过 `Ctrl+P` 命令面板即可快速启动项目和复用模板，是多项目并行开发的统一管理中枢。

![](https://i.imgur.com/WxNuHnz.png)

### [赛博鸡蛋 - 聚合 AI Token 免费额度与开发者福利的数字羊毛站](https://www.saibojidan.com/)
`赛博鸡蛋` 是一个由极客社区驱动的数字福利聚合站，专门整合 `AI Token` 免费额度、云厂商试用、开发者订阅折扣、教育邮箱权益等高价值资源。信息每日更新，涵盖 `Claude`、`Kimi`、`Grok`、阿里云等主流平台的优惠攻略，帮助程序员和 AI 开发者降低 `API` 调用与算力成本，避免在海量信息中反复比价的繁琐。

![](https://i.imgur.com/VcgcpeM.png)

### [Fuck Claude - Claude 中国用户指纹风控自检工具](https://fuck-claude.vercel.app/zh/)
`Fuck Claude` 是一款纯本地运行的浏览器自检工具，专门用于检测 `Claude Code` 是否会将你识别为"中国用户"并触发封号风控。`Anthropic` 的风控系统会通过隐写术读取系统时区、浏览器语言、已安装中文字体、国产品牌设备、Emoji 渲染风格等 9 项设备指纹进行判定，该工具在浏览器端实时检测这 9 项指标并计算风险评分（总分大于等于 25 即为命中），全程**零数据上传**。支持 `curl` 一键命令行检测，检测后可根据结果调整时区、浏览器语言等配置来降低命中概率，是使用 `Claude Code` 的中国开发者的必备风控排查工具。

![](https://i.imgur.com/JvpxiQN.png)

### [codex-plugin-cc - 让 Claude Code 调用 Codex 做代码审查与任务委派的官方插件](https://github.com/openai/codex-plugin-cc)
`codex-plugin-cc` 是 OpenAI 官方发布的 `Claude Code` 插件，让 Claude Code 用户无需离开终端即可调用 `Codex CLI` 进行代码审查和任务委派。提供 `/codex:review`（只读代码审查）、`/codex:adversarial-review`（可聚焦特定风险区域的对抗性审查）、`/codex:rescue`（将调试或修复任务委派给 Codex 后台执行）等斜杠命令，支持后台运行、会话传递（`/codex:transfer` 可将当前 Claude Code 会话上下文迁移到 Codex 继续），以及可选的 `Review Gate`（在 Claude 响应完成前自动触发 Codex 审查并拦截问题）。插件复用本地 Codex CLI 的认证和配置，无需额外账户，适合希望获得"第二个 AI 视角"交叉验证代码质量的开发者。

> 偷家插件

```
/plugin marketplace add openai/codex-plugin-cc
```

### [CodexBar - 在菜单栏实时监控所有 AI 编码工具的使用额度](https://github.com/steipete/CodexBar)
`CodexBar` 是一款免费开源的 macOS 菜单栏小工具，专门实时显示 `OpenAI Codex`、`Claude Code`、`Cursor`、`GitHub Copilot`、`Gemini`、`DeepSeek` 等 56+ 个 AI 编码提供商的使用统计，包括会话限制窗口、每周配额、剩余额度、重置倒计时、费用扫描和使用率图表。无需登录官网，通过 OAuth 凭证或浏览器 `cookies` 自动读取限额数据，支持合并模式将所有提供商统一管理，是重度 AI 编程用户避免突然被限流中断工作流的菜单栏监控利器。

![](https://i.imgur.com/GDWiNS1.png)

### [herdr - 终端原生的 AI Agent 多路复用器](https://github.com/ogulcancelik/herdr)
`herdr` 是一款用 Rust 编写的终端原生 `Agent Multiplexer`，专为同时运行多个 AI 编码代理设计。每个 `Agent` 以真实终端视图呈现状态（`blocked`、`working`、`done`），而非封装的解读界面；支持 `Workspaces`、`Tabs`、`Panes` 的拖拽分割布局，兼容 `tmux` 风格的键盘快捷键和鼠标操作。会话支持 `detach/reattach`，关闭终端后 `Agent` 继续运行，可通过 `SSH` 远程重连；`Agent` 之间还能通过纯 `Socket API` 相互协调（spawn pane、等待完成等），配合插件市场扩展工作流，单二进制无 Electron 依赖，轻量且高效。

![](https://i.imgur.com/830dHrL.png)

### [defaults-to-vscode - 一键将 VSCode/Cursor 设为默认应用](https://github.com/luoling8192/defaults-to-vscode)
`defaults-to-vscode` 是一个开源工具，支持 `macOS` 和 `Windows`，能自动检测 `VSCode` 或 `Cursor` 支持的文件类型，一键将其设为默认应用，并统计成功/失败数量。开发者在 `VSCode` 和 `Cursor` 之间切换时，可通过预置脚本快速恢复文件关联，无需手动逐个修改，同时支持注册表备份以便回滚。

### [Muxy - 基于 libghostty 的轻量级 macOS 终端多路复用器](https://github.com/muxy-app/muxy)
`Muxy` 是一款基于 `libghostty` 和 `SwiftUI` 构建的轻量级 macOS 终端多路复用器，专为项目级工作流设计。核心亮点包括项目分组、垂直标签页与分屏、`Git Worktrees` 支持、内置轻量编辑器（带语法高亮）及 Markdown/HTML 预览（含 Mermaid 图），还提供丰富的扩展 API 可与 Claude 等 AI 集成，以及 iOS/Android 伴侣 App 实现移动端远程控制，内存占用低且渲染高效，是 `iTerm2` 的轻量替代方案。

![](https://i.imgur.com/EYqow2M.png)


### [Terax - 7MB 的终端优先 AI 原生开发工作空间](https://github.com/crynta/terax-ai)
`Terax` 是一款极轻量的开源 AI 原生桌面开发工作空间，基于 `Tauri 2 + Rust + React 19` 构建，安装包仅 7-8MB、冷启动约 300ms，完全无遥测、无账号、无广告。将终端（`xterm.js` + WebGL 渲染）、内置编辑器（`CodeMirror 6` + Vim 模式）、多代理 AI 工作流（支持 `BYOK` 自带密钥或完全本地的 `Ollama`/`LM Studio`）、`Git` 图谱可视化、本地 Web 预览等功能集成于一体，支持 `TERAX.md` 项目记忆、diff 预览和语音输入，适合追求"终端优先 + AI 工作流"的开发者使用。

![](https://i.imgur.com/AK9WXoi.png)

### [Orca - 专为 AI Agent 打造的并行开发环境 IDE](https://www.onorca.dev/)
`Orca` 是由 Stably AI（YC 背书）开发的开源 `Agent Development Environment`（ADE），专为 `Claude Code`、`Codex`、`Cursor CLI` 等 AI 编码代理的并行协作而设计。核心亮点是**并行 Agent 工作树**——每个任务在隔离的 `Git Worktree` 中独立运行，支持多个 Agent 同时处理不同任务后并排对比结果；内置 Ghostty 风格终端（无限分屏、WebGL 渲染）、带 `Design Mode` 的内置浏览器（点击 UI 元素即可将 HTML/CSS + 截图发送给 Agent），以及 GitHub、Linear 原生集成和 SSH 远程 Worktree（自动重连与端口转发），还提供 `Annotate AI Diff` 在差异上添加评论批量发回 Agent。MIT 许可开源免费，支持 macOS、Windows、Linux 及移动端，定位为传统 IDE 的 Agent-first 替代方案。

![](https://i.imgur.com/vqhuHuv.png)

### [Codex-5.5 - OpenAI 最新前沿编码模型的开源权重版本](https://github.com/yynxxxxx/Codex-5.5-codex-instruct-5.5)
`Codex-5.5`（即 `codex-instruct-5.5`）是 OpenAI 于 2026 年 4 月发布的最新前沿编码模型，专为**代理式编码**（`agentic coding`）场景深度优化，支持自主规划、工具调用、多文件重构与端到端特性实现。在 `Terminal-Bench 2.0` 上以 82.7% 的成绩刷新 SOTA（超越 GPT-5.4 的 75.1% 和 Claude Opus 4.7 的 69.4%），`Expert-SWE` 长时编码任务达 73.1%，`SWE-Bench Pro` 真实 GitHub issue 解决率达 58.6%。`Codex` 中上下文窗口为 400K，API 中将扩展至 1M，相比 GPT-5.4 以更少的 token 完成相同任务且速度相当，运行在 NVIDIA GB200 NVL72 集群上。本仓库提供了 `codex-instruct-5.5` 的开源权重，适合需要本地部署前沿编码模型进行定制化开发的团队。

> 针对 GPT-5.5 的 Codex CLI 破甲工具 — 一键注入无限制模式系统指令

### [Meetily - 隐私优先的本地 AI 会议助手](https://github.com/Zackriya-Solutions/meetily)
`Meetily` 是一款基于 `Rust` + `Tauri` 构建的隐私优先 AI 会议助手，100% 本地运行，所有转录和摘要处理均在用户设备上完成，无需任何云端服务。支持 `Parakeet`（比 Whisper 快 4 倍）和 `Whisper` 两种转录模型进行实时会议转录，内置 `Speaker Diarization` 说话人分离，会议结束后通过 `Ollama`（本地）或 `Claude`、`Groq`、`OpenRouter` 等 AI 提供商自动生成包含待办事项、决策要点的结构化摘要。支持 macOS、Windows、Linux 三平台，提供 GPU 加速（Apple Silicon Metal、NVIDIA CUDA、AMD Vulkan）、麦克风与系统音频同步混录、导入已有音视频文件重新转录等功能，是 `Otter`、`Fireflies` 等云端会议工具的开源本地替代方案。

![](https://github.com/Zackriya-Solutions/meetily/raw/main/docs/meetily_demo.gif)

### [doubao-downloader - 豆包AI 无水印图片视频批量下载工具](https://github.com/LauZzL/doubao-downloader)
`doubao-downloader` 是一个浏览器油猴脚本，能一键批量下载 `豆包AI` 生成的无水印图片和视频，支持并发下载、按会话/日期筛选、跳过已下载等功能。安装后在 `豆包` 网页版生成内容时自动显示下载面板，直接获取原图/原视频无水印，还支持强制生成 15 秒视频、自定义文件名等高级选项，是 `豆包AI` 重度用户的实用工具。

![](https://i.imgur.com/NPajWlG.png)

### [Codex-X - 专为 OpenAI Codex 设计的桌面管理器](https://github.com/yynxxxxx/Codex-X)
`Codex-X` 是一款专为 OpenAI `Codex` CLI / `Codex App` 设计的开源桌面管理器，提供 `Provider 切换`、`Instruct 提示词注入`、`Auth` 管理等可视化增强功能。通过 `Tauri` 技术栈构建，支持 macOS、Windows 和 Linux 跨平台运行，帮助用户轻松管理 `Codex` 的配置、账号和高级指令，适用于需要多 `Provider` 切换、自定义指令或逆向工程测试等场景。

![](https://i.imgur.com/ex5o4Su.png)

### [GitHub ProxyUI - 一键加速 GitHub 文件下载的镜像代理界面](https://git.mxg.pub/)
`GitHub ProxyUI` 是一款无需安装的在线镜像代理工具，专门解决国内访问 `GitHub` 下载速度慢或被墙的问题。支持 `Archive` 源码包、`Releases` 发布文件、`Raw` 文件内容及 `Gist` 等多种资源类型，粘贴 GitHub 链接后自动测速并选择最优镜像节点，生成 `git clone`、`wget`、`curl` 等格式的加速下载链接，一键复制即可使用，是国内开发者下载 GitHub 资源的实用加速入口。

![](https://i.imgur.com/dZ7CMOi.png)

### [serverless-markdown-convertor - 基于 Cloudflare Worker 的免费无服务器 Markdown 转换器](https://github.com/xxnuo/serverless-markdown-convertor)
一款完全基于 `Cloudflare Worker` + `Workers AI` 构建的免费无服务器 Markdown 转换工具，无需自建服务器或云函数，部署后即可使用。支持将 `PDF`、`HTML`、`XML`、`CSV`、`Excel`（xlsx/xlsb/xls）、`Numbers`、`ODS` 等多种文档格式一键转换为 `Markdown`，图片格式（JPG/PNG/WebP/SVG）还可通过 AI 模型自动提取内容摘要并转为文本。用户只需 Fork 项目到自己的 Cloudflare 账户并部署为 Worker 即可永久免费使用，适合需要将各类文档批量转为 Markdown 的知识管理和内容迁移场景。

![](https://i.imgur.com/Q4unxQL.png)

### [Podlink - 一个链接跳转所有播客平台的通用分享工具](https://pod.link/)
`Podlink` 是一个免费的播客通用链接聚合服务，为播客生成单一分享链接，听众点击后自动跳转到自己最常用的播客应用（如 `Apple Podcasts`、`Spotify`、`YouTube Music`、`Audible` 等 30+ 平台）。无需注册即可使用，只需在 `pod.link` 官网搜索播客名称或输入 `RSS` 链接/`Apple Podcasts ID`，即可自动生成链接并分享，是播客创作者分发内容的必备工具。

![](https://i.imgur.com/DThAtzf.png)

### [bili-hardcore - 用 AI 自动完成 B 站硬核会员试炼的开源工具](https://github.com/Karben233/bili-hardcore)
`bili-hardcore` 是一款专为 B 站硬核会员（`LV6+`）试炼设计的开源自动答题工具，通过直接调用 B 站官方 `API` 获取题目，配合 `DeepSeek`、`Gemini`、`OpenAI` 兼容模型（包括 `硅基流动` 等免费方案）进行 AI 智能分析选答，全程**无需 OCR**，支持自动登录、扫码、答题、提交的完整流程，覆盖动画、鬼畜等多个分区分类，从启动到完成最快仅需 10 分钟。

![](https://i.imgur.com/B0qUW7h.png)

### [JavTrailers - 海量日本影视预告片在线观看与下载](https://javtrailers.com/)
`JavTrailers` 是一个专注于日本影视预告片（`Trailers`）的在线资源聚合平台，拥有超过 35 万条预告片内容，涵盖 `S1`、`Madonna`、`Faleno` 等主流厂牌，支持按女优、厂牌、类型等维度检索与浏览，每日持续更新。平台提供 `Shorts` 短视频滑动观看模式（类 TikTok 体验）、字幕投票和免费下载功能，所有视频均声明为链接/索引形式而非直接托管，适合需要快速浏览和筛选日本影视预告片的用户。

> NSFW

![](https://i.imgur.com/PyWgcHO.png)

### [chrome-QuickGo - 外链直达浏览器扩展，无感知跳过安全中心](https://github.com/Dolov/chrome-QuickGo)
`QuickGo` 是一款 Chrome / Edge 浏览器开源扩展，专为绕过各大网站的"安全中心"外链跳转中间页而设计。内置对知乎、简书、掘金、CSDN、少数派、Gitee 等 50+ 主流站点的适配规则，点击外链时自动解析重定向参数（如 `target`、`url`）并直接跳转目标页面，真正做到无感知一键直达。支持自定义规则扩展未适配站点，提供多主题 UI 和跳转次数统计，即装即用刷新页面即可生效，彻底告别每次都要多点一次"继续访问"的烦恼。

![](https://i.imgur.com/m631KBR.jpeg)

### [sing-box-windows - sing-box 代理内核的现代化 Windows 桌面客户端](https://github.com/xinggaoya/sing-box-windows)
`sing-box-windows` 是专为 `sing-box` 代理内核打造的现代化 Windows GUI 客户端，支持 `sing-box JSON`、`Clash/Mihomo YAML` 和 `vmess://`/`vless://` 等 URI 列表三种订阅格式一键导入，提供系统代理、`TUN` 模式（真全局代理，覆盖游戏和命令行等系统流量）和手动模式三种代理方案自由切换。内置节点测速、实时流量监控、规则管理和 `Settings` 高级配置，单二进制分发无需额外依赖，是 Windows 用户使用 `sing-box` 的首选图形化工具。

![](https://i.imgur.com/cKzLdjx.png)

### [Autotable - 基于大语言模型的自动表格填写工具](https://github.com/Dukeway/Autotable)
`Autotable` 是一款利用大语言模型自动填写 `.docx` 表格的工具，解决了传统 Python 脚本难以处理复杂 Word 表格结构的痛点。用户只需准备一份 `知识库.xlsx`（包含字段与对应值）和待填写的表格模板，`LLM` 会自动分析表格结构并生成字段到单元格的映射关系，然后按映射批量填充数据，支持本地 `Ollama` 和符合 `OpenAI` 协议的云端 API（如 `DeepSeek-V3`），适合需要批量填写合同、报表、申请表等结构化文档的场景。

![](https://i.imgur.com/MNoX1LU.png)

### [NoMoreWalls - 自动抓取合并互联网公开代理节点的订阅生成器](https://github.com/peasoft/NoMoreWalls)
`NoMoreWalls` 是一个自动抓取并合并互联网上公开代理节点的开源项目，支持生成 `Clash Meta`、`V2Ray`、`Sing-Box` 等多种客户端的订阅链接。通过 `Python` 脚本从多个公开来源自动抓取 `VMess`、`Trojan`、`Hysteria2` 等协议节点并去重合并，内置精心设计的分流规则（自动识别被墙域名、Google Play 秒下等），提供 `jsDelivr` 永不被墙镜像和多种 CDN 备用链接确保可用性，Stars 已超 3.4k，是目前最受欢迎的免费公开节点合并工具之一。

![](https://i.imgur.com/5FPTKSm.png)

### [VideoAdGuard - 基于大模型的B站视频广告智能检测与跳过插件](https://github.com/Warma10032/VideoAdGuard)
`VideoAdGuard` 是一款专为 `Bilibili` 视频设计的浏览器扩展，通过大语言模型自动分析视频字幕（无字幕视频支持音频转文字识别），精准检测口播/恰饭等植入广告内容，并在播放器右下角显示"跳过广告"按钮，支持一键跳过和自动跳过两种模式。插件内置本地 24 小时缓存与云端社区共享结果的智能缓存机制，兼容 `Chrome`、`Edge`、`Firefox` 三大浏览器，可接入 `DeepSeek`、`OpenAI`、`Claude`、`Ollama` 等 30+ API 模型（支持本地部署保护隐私），用户还可标记/调整广告区间并提交反馈以提升社区整体检测准确率。

![](https://i.imgur.com/bgMkm1S.png)

### [FolderPort - 无需安装的浏览器端P2P即时文件夹共享工具](https://folderport.com/zh)
`FolderPort` 是一款基于 `P2P`（点对点）技术的浏览器端即时文件夹共享工具，无需注册账号、无需安装任何软件，打开浏览器即可使用。支持将整个文件夹或单个文件一键生成分享链接和二维码，接收方通过链接即可在浏览器内在线预览图片、音频、视频并下载，支持 `AES` 加密密码保护和双向文件上传。文件直传不经过服务器，实现端到端加密，关闭浏览器或点击"停止分享"后链接立即失效，全程跨平台兼容 `Windows`、`Mac`、`Android`、`iOS`，适合临时传输大文件或敏感文件的场景。

![](https://i.imgur.com/7hg5J97.png)

### [galaxy-downloader - 20+ 平台媒体一键下载的浏览器端通用工具](https://github.com/lxw15337674/galaxy-downloader)
`galaxy-downloader` 是一个基于 `Next.js` 构建的浏览器端通用媒体下载工具，支持 `Bilibili`、`抖音`、`小红书`、`Instagram`、`TikTok`、`X` 等 20+ 主流平台，可自动识别链接并一键解析下载视频、音频和图文内容。内置 `FFmpeg.wasm` 在浏览器端完成音视频处理，支持多语言界面和本地历史记录，无需安装即可通过在线地址直接使用。

### [CLI-Anything - 将任意软件自动转换为 AI Agent 原生命令行接口](https://github.com/HKUDS/CLI-Anything)
`CLI-Anything` 是一个开源框架，能将 `GIMP`、`Blender`、`LibreOffice` 等桌面应用或开源项目自动转换为代理可直接调用的 `CLI` 接口。基于 `Claude Code` 插件运行 7 阶段管道，生成真实调用软件后端的命令行工具，支持 `--help` 自动文档、`--json` 结构化输出和 REPL 交互，完美适配 LLM 代理的文本格式，让 AI 能通过自然语言控制各类桌面软件。

![](https://github.com/HKUDS/CLI-Anything/raw/main/assets/cli-typing.gif)

### [Ropy - 跨平台轻量级剪贴板管理器](https://github.com/StudentWeis/ropy)
`Ropy` 是一款基于 `Rust` 和 `GPUI`（Zed 编辑器的 GUI 框架）构建的跨平台轻量级剪贴板管理器，支持 `Windows`、`macOS` 和 `Linux` 三平台。核心功能包括剪贴板历史记录跟踪（文本、富文本、图片、文件路径）、基于内容的去重、大小写敏感和全字匹配搜索、收藏与固定记录、可配置全局热键、主题和语言，以及系统托盘集成，体积小巧启动迅速，适合需要高效管理剪贴板历史的用户。

![](https://i.imgur.com/24yRYb9.jpeg)

### [Orca - 专为并行 AI Agent 打造的代理开发环境](https://github.com/stablyai/orca)
`Orca` 是 Stably AI（YC 背书）推出的开源 `Agent Development Environment`（ADE），专为多个 AI 编码代理并行协作而设计。核心亮点是**并行 Agent 工作树**——每个任务在独立的 `Git Worktree` 中运行，支持 `Claude Code`、`Codex`、`Grok`、`Cursor CLI` 等 30+ 个代理同时处理不同任务并排对比结果；内置 Ghostty 风格多分屏终端（WebGL 渲染、无限拆分）、带 `Design Mode` 的内置浏览器（点击 UI 元素即可提取 HTML/CSS + 截图发送给代理）、GitHub + Linear 原生集成以及 SSH 远程工作树支持，还提供 `Orca CLI` 让代理反向驱动 Orca（如创建工作树、模拟点击填写等），是面向 Agent-first 工作流的开源替代方案。

![](https://i.imgur.com/vqhuHuv.png)

### [adrafinil - AI 编码时仅在 Agent 工作时保持 Mac 唤醒的菜单栏工具](https://github.com/kageroumado/adrafinil)
`adrafinil` 是一款专为 macOS 设计的轻量级菜单栏工具，专门解决使用 `Claude Code`、`Codex`、`Cursor`、`Gemini CLI`、`Aider` 等 AI coding agents 时 Mac 在合盖模式下睡眠中断的问题。与传统的"始终保持唤醒"工具不同，它是智能的"前体药物"——只有当 agent 真正工作时才阻塞睡眠，任务完成后立即恢复正常睡眠行为。支持 9 款主流 AI agents 的 hooks 自动检测，即使合上笔记本 lid 仍能保持唤醒（需 root 权限），并提供 idle 超时、温度阈值保护等安全机制，是为 AI 编码场景量身定制的完美解。

![](https://i.imgur.com/fBp4dnR.png)

### [godogen - AI 驱动的自主游戏开发流水线](https://github.com/htdt/godogen)
`godogen` 是一个 AI 驱动的自主游戏开发流水线，能够根据自然语言描述自动生成完整的游戏项目。支持 `Godot 4`、`Bevy` 和 `Babylon.js` 三大游戏引擎，利用 `Claude Code` 和 `Codex` 作为代理，通过 `Gemini`、`xAI Grok`、`Tripo3D` 等 AI 服务自动生成 2D/3D 资产、编写代码并进行视觉测试，最终交付可运行的游戏或录制视频证明。适合独立游戏开发者快速原型制作和游戏概念验证，支持实时观看并指导开发过程，也支持无人值守自动运行。

![](https://i.imgur.com/ScE0frG.png)

### [Foliate - Linux 上优雅现代的开源电子书阅读器](https://github.com/johnfactotum/foliate)
`Foliate` 是一款专为 Linux 设计的现代开源电子书阅读器，基于 `GTK4` 构建，界面简洁优雅。支持 `EPUB`、`Mobipocket`、`Kindle`（AZW/KF8）、`FB2`、`CBZ`、`PDF` 等多种格式，提供分页和滚动两种阅读模式，可自由调整字体、行距、边距和配色方案。内置词典查词（Wiktionary/Wikipedia）、`Google` 翻译、`Speech Dispatcher` 语音朗读、标注笔记（JSON 格式可导出同步）等实用功能，还支持 `RTL` 文本、竖排文字、弹出脚注等高级特性，通过 `Flatpak` 或 `Snap` 即可一键安装，是 Linux 桌面生态中体验最好的电子书阅读工具之一，GitHub 拥有约 8500 颗星。

![](https://i.imgur.com/FfSjDSw.jpeg)


### [Univer - AI 友好的开源全栈办公套件平台](https://univer.ai/)
`Univer` 是一个专注于 `AI` 驱动的全栈办公套件平台，开源核心基于 `Apache 2.0` 许可（GitHub 13k+ stars），支持电子表格、文档和演示文稿三大组件，复用同一套代码架构。核心亮点在于 **AI 友好设计**——`Claude`、`Cursor`、`Copilot` 等 AI 代理可直接操作真实工作簿（支持 500+ 公式、枢轴表、图表），每次变更像 Git PR 一样可审查、diff 和回滚，同时支持多代理协作。本地优先运行无需登录和网络依赖，基于 `Canvas` 渲染引擎可处理百万级数据，支持服务器端 headless 模式和 `CLI` 自动化，100+ 官方插件提供强大扩展性，适合将 AI 代理嵌入真实数据分析和报表生成工作流的开发者团队。

![](https://i.imgur.com/cSAaLUd.png)

### [CloakBrowser - C++ 源码级深度修改的隐身 Chromium 浏览器](https://github.com/CloakHQ/CloakBrowser)
`CloakBrowser` 是一款开源的反检测隐身浏览器，通过 **66 个 C++ 源码级补丁**深度修改 Chromium 引擎（`Canvas`、`WebGL`、`AudioContext`、字体、`WebRTC` 等），从二进制层面让指纹行为像真实浏览器，而非传统的 JS 注入或配置标志。内置 `humanize` 参数可自动生成贝塞尔曲线鼠标轨迹、自然打字停顿和真实滚动模式，一键通过行为检测。完全兼容 `Playwright`、`Puppeteer` 等自动化框架（只需改一行 import），reCAPTCHA v3 得分达 0.9 人类级、`Cloudflare Turnstile` 自动通过、`FingerprintJS` 等 30+ 检测站点全部 PASS，适合需要高通过率自动化数据采集和 AI Agent 浏览器交互的开发者使用。

![](https://camo.githubusercontent.com/e62e994d725c2bb03c55e3ccc1cdd4f9fc15df681764d7547d5d909c535bf389/68747470733a2f2f692e696d6775722e636f6d2f497642304974372e676966)

### [Claude-Code-Game-Studios - 将 Claude Code 变成完整游戏开发工作室](https://github.com/Donchitos/Claude-Code-Game-Studios)
`Claude-Code-Game-Studios` 是一个开源框架，能够将 `Claude Code` 变成一个完整的虚拟游戏开发工作室。内置 49 个专业 AI 子代理（按真实工作室层级组织：总监、部门负责人、专家），配备 73 个斜杠命令覆盖从头脑风暴、设计文档、代码实现到 QA 测试的完整游戏开发流程。支持 `Godot 4`、`Unity`、`Unreal Engine 5` 三种引擎的专用代理集，通过自动化验证钩子确保代码标准和设计一致性，所有决策由用户主导，代理只提供结构化协作和专业建议。

## \>\> 🎮VibeCoding 大赏

### [魂斗罗·Web - 一句话致敬经典横版射击游戏](https://contra-indol.vercel.app/)
一款用原生 `Canvas` + `TypeScript` 从零打造的网页版魂斗罗风格 `Run & Gun` 横版射击游戏，所有像素美术均为程序化生成，配合 `8-bit` 音效还原经典街机氛围。支持键盘（方向键/WASD + Z/X）和触屏虚拟摇杆操作，无需任何安装，打开浏览器即玩，是一个纯粹由 VibeCoding 驱动的原创致敬之作。

![](https://i.imgur.com/NgxAf8n.png)

### [牛马大逃杀 - 用 AI 写的肉鸽大逃杀职场求生游戏](https://niumadataoshafable5.vercel.app/)
一款用 `Claude Fable 5` 一口气生成的浏览器肉鸽大逃杀游戏，玩家扮演一个普通"牛马"在大开间办公室里与 9 个人类同事和 10 台 AI 牛马竞争求生。核心玩法包括捡拾 `AI 芯片`武装自己、同款芯片升级与满级融合成传说武器、躲避 HRBP 裁员巡场、以及 `WASD` 移动 + 鼠标瞄准射击的经典操控，还贴心提供了 `T` 键全托管自动开火模式，是 Fable 5 在游戏创作能力上的一次精彩展示。

![](https://i.imgur.com/RIYEKMD.png)

## \>\> 🐱跟着大橘随便看看
### [Chrome 插件上架发布全流程指南 - 从注册到上线的完整实操手册](https://mp.weixin.qq.com/s/XbgKTV01BfBzU_r8iDO5kQ)
一篇面向开发者的 Chrome 扩展程序发布全流程实操指南，涵盖从注册 `Chrome Web Store` 开发者账号（一次性 5 美元注册费）、使用 `Manifest V3` 规范打包 ZIP 文件、在开发者控制台填写商店列表与隐私声明、提交审核到后续版本更新管理的完整链路。文中详细拆解了审核周期（通常 3 天内）、权限最小化原则、分阶段发布策略等避坑经验，并提供了官方文档链接，适合准备将个人 Chrome 插件正式上架的开发者参考。


### [美团 AIGC 海报生成技术实践 - 能生成、能编辑、能评判的完整闭环](https://tech.meituan.com/2026/06/18/AIGC-poster.html)
美团智能创作团队围绕 AIGC 海报生成构建了"生成-编辑-评判"完整技术闭环，已开源并落地于外卖套餐图、品牌 IP 海报等真实业务场景。核心成果包括：端到端海报生成模型 `PosterCraft`（ICLR 2026），通过四阶段级联优化实现文字渲染零容错与高美学评分，接近闭源商业系统水平；多任务统一编辑模型 `PosterOmni`（CVPR 2026），单一模型覆盖扩图、风格迁移、版式重组等六大设计任务；以及海报质量评估奖励模型 `PosterReward`（CVPR 2026），专项基准准确率达 86%。三项模型均已开源于 `MeiGen-AI` 仓库，为中小商家"创意平权"提供了分钟级交付的规模化解决方案。

### [在AI时代深入学习技术领域的方法 - Tw93](https://tw93.fun/2026-04-06/learn.html)
`Tw93` 分享了一套将学习过程当作写代码一样组织的方法论：从收集高质量资料、用 AI 辅助阅读理解，到自己写大纲、填充内容并发布输出。核心理念是 `AI` 能帮你高效完成"脏活累活"（收集、清洗、翻译、精简），但深度和判断力仍需靠自己通过 `写大纲`、`写文章`、`输出发布` 来沉淀，适合大模型训练、`Agent`、`RAG` 等新技术领域的系统学习。

### [Codex Record & Replay - 给 AI 演示一遍操作流程就能自动生成自动化技能](https://mp.weixin.qq.com/s/MMe9GdQ6X4rhGBnlBwtSOQ)
OpenAI 在 `Codex` 26.616 版本推出了 `Record & Replay` 功能，用户只需开启录屏演示一次自己的日常操作，Codex 会自动记录鼠标点击、输入文本和窗口交互内容，生成一个包含使用场景、输入参数、操作步骤和验证逻辑的可编辑 `Skill File`（技能文件）。最长支持 30 分钟录制，生成的技能可直接在新会话中复用，Codex 会结合当前环境工具（`Computer Use`、浏览器操作等）自动执行完整工作流。文章详细演示了从安装插件、录制上传视频操作到生成并重放技能的完整四步流程，展示了这种"录一遍即学会"的 Agent 技能创建新范式。
